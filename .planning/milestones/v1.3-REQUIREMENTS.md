# Requirements Archive: Proto Sender v1.3

**Milestone:** v1.3 Publishing UX + Message Blocks
**Status:** ✅ SHIPPED 2026-05-20
**Defined:** 2026-05-19 | **Archived:** 2026-05-20

---

## v1.3 Requirements — All Complete (16/16)

### Publishing Enhancements

- [x] **PUBL-01**: User can see routing key suggestions populated from live RabbitMQ exchange bindings when an exchange is selected in the publish bar
- [x] **PUBL-02**: User sees no routing key suggestions for `headers` and `fanout` exchanges (autocomplete suppressed — routing key is always empty for these types)
- [x] **PUBL-03**: Topic exchange wildcard binding patterns (e.g. `orders.*.created`) are shown in suggestions labeled as patterns so the user knows to edit before sending
- [x] **PUBL-04**: Routing key input falls back to plain free-text when the Management API is unavailable — no error state shown
- [x] **PUBL-05**: User sees a green ACK badge in the publish bar after the broker confirms delivery; badge auto-dismisses after 3 seconds
- [x] **PUBL-06**: User sees an amber Returned badge when a published message has no route (mandatory=true, no binding match); badge auto-dismisses after 5 seconds
- [x] **PUBL-07**: User sees a red NACK badge when the broker negatively acknowledges a message; badge auto-dismisses after 5 seconds
- [x] **PUBL-08**: User sees a gray Timeout badge when broker confirmation does not arrive within 5 seconds; badge requires manual dismiss

### Message Blocks

- [x] **BLK-01**: User can open and close a block library panel from a toggle button in the FormPanel header
- [x] **BLK-02**: User can create a named block by entering a name and writing a JSON object (key-value pairs) in a CodeMirror editor
- [x] **BLK-03**: User can edit an existing block's name and JSON content
- [x] **BLK-04**: User can delete a block with a confirmation prompt
- [x] **BLK-05**: Blocks persist across app restarts (saved via tauri-plugin-store)
- [x] **BLK-06**: User can apply a block to the current form by dragging it from the block library panel and dropping it onto the form
- [x] **BLK-07**: Block merge fills only empty (unmodified/not-dirty) form fields — never overwrites a field the user has already edited
- [x] **BLK-08**: User sees a warning toast listing field names from the block that had no matching field in the current form

## Future Requirements (carried forward to next milestone)

### Publishing

- **PUBL-F-01**: Routing key autocomplete suggestions for exchanges accessible only through Management API pagination (> 100 bindings)
- **PUBL-F-02**: Publisher confirms timeout duration configurable per connection profile

### Message Blocks

- **BLK-F-01**: Save current form state as a block (capture-from-form shortcut)
- **BLK-F-02**: Block tagging / categories for organizing large block libraries
- **BLK-F-03**: Import / export blocks as JSON file

## Out of Scope

| Feature | Reason |
|---------|--------|
| Type-safe blocks scoped to a proto message type | Research showed global/type-agnostic with field-name matching meets the need; typed blocks add schema-coupling complexity without clear benefit for a dev tool |
| `mandatory: false` as user toggle in v1.3 | Defaulting to `true` is the right behavior; per-send toggle can be added in a future milestone if teams need it |
| Block merge override (overwrite existing values) | Fill-empty-only is the safe default; an override mode can be added later if teams request it |
| Per-field drop targets in the form | Single form-level drop zone is sufficient; per-field DnD adds complexity without clear UX benefit |
| Real-time binding updates (live refresh on exchange selection) | Fetch-once-on-selection is sufficient; binding tables rarely change during a session |

## Traceability

| Requirement | Phase | Status | Delivered |
|-------------|-------|--------|-----------|
| PUBL-01 | Phase 9 | ✅ Complete | RoutingKeyCombobox + fetch_bindings + exchange type badge combobox suppression |
| PUBL-02 | Phase 9 | ✅ Complete | Autocomplete suppressed for headers/fanout exchanges |
| PUBL-03 | Phase 9 | ✅ Complete | Pattern suggestions labeled with exchange type badge |
| PUBL-04 | Phase 9 | ✅ Complete | ManagementApiUnavailable → silent fallback to plain text input |
| PUBL-05 | Phase 10 | ✅ Complete | Green ACK badge, 3s auto-dismiss via useRef timer |
| PUBL-06 | Phase 10 | ✅ Complete | Amber Returned badge, 5s auto-dismiss, mandatory=true |
| PUBL-07 | Phase 10 | ✅ Complete | Red NACK badge, 5s auto-dismiss |
| PUBL-08 | Phase 10 | ✅ Complete | Gray Timeout badge, manual dismiss required |
| BLK-01 | Phase 11 | ✅ Complete | Library toggle button in FormPanel header, panel collapses/expands |
| BLK-02 | Phase 11 | ✅ Complete | CodeMirror editor, name input, Save button |
| BLK-03 | Phase 11 | ✅ Complete | Edit view pre-populates name + JSON content |
| BLK-04 | Phase 11 | ✅ Complete | AlertDialog delete confirmation |
| BLK-05 | Phase 11 | ✅ Complete | tauri-plugin-store persistence, hydration gate |
| BLK-06 | Phase 12 | ✅ Complete | dnd-kit draggable blocks → form drop zone |
| BLK-07 | Phase 12 | ✅ Complete | dirtyFields guard — only non-dirty fields filled |
| BLK-08 | Phase 12 | ✅ Complete | Sonner warning toast listing unmatched field names |

**Coverage:** 16/16 requirements complete ✓

---
*Requirements defined: 2026-05-19 | Archived: 2026-05-20 after milestone close*
