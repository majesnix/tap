---
phase: "23"
plan: "03"
type: execute
wave: 3
depends_on:
  - "23-01"
  - "23-02"
files_modified:
  - src/components/plans/PlanDetailPanel.tsx
  - src/components/plans/StepListPanel.tsx
autonomous: true
requirements:
  - RESP-04
  - RESP-05

must_haves:
  truths:
    - "D-01: Decoded step reply appears in the StepFieldEditor right pane (PlanDetailPanel's editor area), not in the step list — StepListPanel remains compact"
    - "D-02: When paneMode === 'reply' and the selected step has a stored reply, StepReplyView replaces StepFieldEditor in the editor TabsContent — not appended alongside"
    - "D-03: Second click on the same step row while paneMode === 'reply' calls setPaneMode('editor') and returns — toggling back to editor mode"
    - "D-05: The [Step Editor | Reply Feed] tab strip appears in PlanDetailPanel only after hasRunStarted — derived from Object.keys(stepStatuses).length > 0 || planReplyFeed.length > 0"
    - "D-07: Reply Feed tab does not auto-activate on run start — activeTab local state starts as 'editor' and is only changed by explicit user interaction"
    - "D-08: The tab strip is always accessible; the Reply Feed tab does not vanish when paneMode === 'reply'"
    - "Pitfall 1: forceMount prop is applied to the Step Editor TabsContent to keep StepFieldEditor mounted during Reply Feed tab display"
    - "Pitfall 4: hasRunStarted uses stepStatuses/planReplyFeed predicates — NOT isRunning or runningPlanId which clear post-run"
    - "Step list reply dot: when stepReplies[step.id] is non-null, a visible dot indicator is rendered on the step row in StepListPanel"
  artifacts:
    - path: src/components/plans/PlanDetailPanel.tsx
      provides: "Tabs wrapper (hasRunStarted-gated) with forceMount on editor tab; StepReplyView/StepFieldEditor toggled by paneMode; PlanReplyFeedTab in reply-feed tab"
    - path: src/components/plans/StepListPanel.tsx
      provides: "Reply indicator dot on step row; toggle click handler replacing plain onSelectStep"
  key_links:
    - from: src/components/plans/PlanDetailPanel.tsx
      to: src/components/plans/StepReplyView.tsx
      via: "conditional render based on paneMode + selectedStepReply"
      pattern: "StepReplyView"
    - from: src/components/plans/PlanDetailPanel.tsx
      to: src/components/plans/PlanReplyFeedTab.tsx
      via: "Reply Feed TabsContent"
      pattern: "PlanReplyFeedTab"
    - from: src/components/plans/PlanDetailPanel.tsx
      to: src/stores/usePlanExecutionStore.ts
      via: "paneMode, stepReplies, planReplyFeed, stepStatuses selectors"
      pattern: "paneMode|stepReplies|planReplyFeed"
    - from: src/components/plans/StepListPanel.tsx
      to: src/stores/usePlanExecutionStore.ts
      via: "stepReplies and setPaneMode from usePlanExecutionStore"
      pattern: "stepReplies|setPaneMode"
---

<objective>
Wire the new store fields and display components into the two-pane layout. PlanDetailPanel gains the tab strip (gated on hasRunStarted) and the paneMode-driven StepReplyView/StepFieldEditor toggle. StepListPanel gains the reply indicator dot and the second-click toggle behavior.

Purpose: Delivers the full RESP-04 and RESP-05 user experience — users can see decoded replies inline and browse the shared reply feed.
Output: Modified PlanDetailPanel.tsx and StepListPanel.tsx. No new files created in this plan.
</objective>

<execution_context>
@/Users/majesnix/.claude/get-shit-done/workflows/execute-plan.md
@/Users/majesnix/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-response-view-inline-and-shared-feed/23-CONTEXT.md
@.planning/phases/23-response-view-inline-and-shared-feed/23-RESEARCH.md
@.planning/phases/23-response-view-inline-and-shared-feed/23-PATTERNS.md

<interfaces>
<!-- Key types and contracts for the executor. Extracted from codebase. -->

From src/components/plans/PlanDetailPanel.tsx (current state — full file read required):
  Props: { selectedPlan: Plan | null }
  Currently imports: usePlanExecutionStore, StepFieldEditor, StepListPanel, PlanRunBar, DndContext, etc.
  Current store reads: const { activeStepId, runningPlanId } = usePlanExecutionStore()
  Currently renders StepFieldEditor directly with step={selectedStep} planId={planId} disabled={isRunning}

From src/components/plans/StepListPanel.tsx (current state — full file read required):
  Current store reads: const { stepStatuses, activeStepId } = usePlanExecutionStore()
  SortableStepRow.onSelect prop: currently () => onSelectStep(step.id)
  StepStatusBadge rendered when stepStatus !== undefined

From src/stores/usePlanExecutionStore.ts (after Plan 01):
  stepReplies: Record<string, ReplyMessage>
  planReplyFeed: FeedMessage[]
  paneMode: 'editor' | 'reply'
  Actions: setStepReply, appendReplyFeedEntry, setPaneMode

From src/components/ui/tabs.tsx (verified):
  TabsContent spreads ...props — forceMount passes through to TabsPrimitive.Content natively. No change needed to tabs.tsx.

From src/components/layout/RightPanel.tsx (Tabs analog — tab state pattern):
  const [activeTab, setActiveTab] = useState<...>('...')  — tab state is LOCAL useState
  Tabs root: <Tabs value={activeTab} onValueChange={(v) => setActiveTab(v as typeof activeTab)}>
  TabsList className: "w-full rounded-none border-b border-border justify-start px-2"

New components from Plan 02:
  StepReplyView — import from "./StepReplyView", props: { reply: ReplyMessage, stepName: string }
  PlanReplyFeedTab — import from "./PlanReplyFeedTab", no props

Tabs imports: import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"
cn import: import { cn } from "@/lib/utils"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tab strip and paneMode-driven reply view to PlanDetailPanel</name>
  <files>src/components/plans/PlanDetailPanel.tsx</files>
  <read_first>
    src/components/plans/PlanDetailPanel.tsx — read the full file to understand current import list, store destructure, effectiveSelectedStepId logic, and the StepFieldEditor render at the bottom before editing
    src/components/layout/RightPanel.tsx — read to confirm the Tabs wiring pattern (TabsList className, activeTab useState, onValueChange signature)
    src/components/plans/StepReplyView.tsx — verify exported function name and props signature from Plan 02
    src/components/plans/PlanReplyFeedTab.tsx — verify exported function name from Plan 02
    src/components/ui/tabs.tsx — already verified: TabsContent spreads ...props, forceMount works without changes
  </read_first>
  <action>
    Add the following imports to PlanDetailPanel.tsx:
      { useState } is already imported — no duplicate needed
      { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"
      { cn } from "@/lib/utils"
      { StepReplyView } from "./StepReplyView"
      { PlanReplyFeedTab } from "./PlanReplyFeedTab"
      type { ReplyMessage } from "@/lib/types"

    Extend the usePlanExecutionStore destructure to include:
      paneMode, stepReplies, planReplyFeed, stepStatuses

    Add a local useState for the active tab:
      const [activeTab, setActiveTab] = useState<'editor' | 'reply-feed'>('editor')
    This must be LOCAL component state — NOT in the global store (tab strip is UI-only; paneMode in the store controls the content inside the editor tab).

    Derive the hasRunStarted predicate (Pitfall 4 — never use isRunning or runningPlanId):
      const hasRunStarted = Object.keys(stepStatuses).length > 0 || planReplyFeed.length > 0

    Derive the selected step's stored reply and name for StepReplyView:
      const selectedStepReply: ReplyMessage | null =
        effectiveSelectedStepId ? (stepReplies[effectiveSelectedStepId] ?? null) : null
      const selectedStepName =
        selectedStep?.name ?? ''

    Replace the current bare StepFieldEditor render (the one inside the right-pane area, after the DndContext/StepListPanel block) with a conditional:

    When hasRunStarted is false: render StepFieldEditor directly as before:
      <StepFieldEditor step={selectedStep} planId={planId} disabled={isRunning} />

    When hasRunStarted is true: render a Tabs component:
      <Tabs value={activeTab} onValueChange={(v) => setActiveTab(v as 'editor' | 'reply-feed')} className="flex flex-col flex-1 min-h-0">
        <TabsList className="w-full rounded-none border-b border-border justify-start px-2">
          <TabsTrigger value="editor" className="text-xs">Step Editor</TabsTrigger>
          <TabsTrigger value="reply-feed" className="text-xs">
            Reply Feed{planReplyFeed.length > 0 ? ` (${planReplyFeed.length})` : ''}
          </TabsTrigger>
        </TabsList>
        <TabsContent
          value="editor"
          forceMount
          className={cn("flex-1 overflow-hidden m-0 p-0", activeTab !== 'editor' && 'hidden')}
        >
          {paneMode === 'reply' && selectedStepReply !== null
            ? <StepReplyView reply={selectedStepReply} stepName={selectedStepName} />
            : <StepFieldEditor step={selectedStep} planId={planId} disabled={isRunning} />}
        </TabsContent>
        <TabsContent value="reply-feed" className="flex-1 overflow-hidden m-0 p-0">
          <PlanReplyFeedTab />
        </TabsContent>
      </Tabs>

    The StepFieldEditor call outside the Tabs (when hasRunStarted is false) requires no change to its props.
    Do not modify PlanRunBar, DndContext, StepListPanel props, or the effectiveSelectedStepId logic.
  </action>
  <verify>
    <automated>cd /Users/majesnix/gits/proto-sender && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - PlanDetailPanel.tsx imports Tabs, TabsContent, TabsList, TabsTrigger from "@/components/ui/tabs"
    - PlanDetailPanel.tsx imports StepReplyView and PlanReplyFeedTab
    - hasRunStarted predicate uses stepStatuses and planReplyFeed — grep: `grep -n "hasRunStarted" src/components/plans/PlanDetailPanel.tsx` returns a match containing "stepStatuses" and "planReplyFeed"
    - hasRunStarted does NOT use isRunning or runningPlanId in its definition — grep: `grep -n "hasRunStarted" src/components/plans/PlanDetailPanel.tsx` output does not contain "isRunning\|runningPlanId"
    - forceMount prop is present on a TabsContent with value="editor" — grep: `grep -n "forceMount" src/components/plans/PlanDetailPanel.tsx` returns a match
    - The 'hidden' className conditional is on the editor TabsContent — grep: `grep -n "hidden" src/components/plans/PlanDetailPanel.tsx` returns a match containing "activeTab !== 'editor'"
    - StepReplyView render is guarded by paneMode === 'reply' && selectedStepReply !== null
    - PlanReplyFeedTab render appears inside the reply-feed TabsContent
    - tsc exits 0 with no new type errors
  </acceptance_criteria>
  <done>PlanDetailPanel shows bare StepFieldEditor before first run; shows tabbed layout with forceMount editor tab and accessible reply-feed tab after first run; inline reply view appears when paneMode === 'reply' and a step reply exists; tsc exits 0</done>
</task>

<task type="auto">
  <name>Task 2: Add reply indicator dot and toggle click handler to StepListPanel</name>
  <files>src/components/plans/StepListPanel.tsx</files>
  <read_first>
    src/components/plans/StepListPanel.tsx — read the full file to understand: current usePlanExecutionStore destructure (line 231), SortableStepRow component definition and props interface, the onSelect handler call site in steps.map(), and the StepStatusBadge render (lines 175-178)
    src/stores/usePlanExecutionStore.ts — confirm stepReplies type and setPaneMode action signature after Plan 01
  </read_first>
  <action>
    In StepListPanel's usePlanExecutionStore destructure (currently `const { stepStatuses, activeStepId } = usePlanExecutionStore()`), add:
      stepReplies, setPaneMode

    Add type import for ReplyMessage at the top-level imports block.

    Add stepReplies as a prop to SortableStepRowProps interface:
      stepReplies: Record<string, ReplyMessage>
    Pass stepReplies down at the SortableStepRow call site in steps.map().

    Replace the plain `onSelect={() => onSelectStep(step.id)}` call at each SortableStepRow with a toggle handler function. The toggle logic (D-03) reads paneMode imperatively via usePlanExecutionStore.getState():
      onSelect={() => {
        const { paneMode } = usePlanExecutionStore.getState()
        if (selectedStepId === step.id && paneMode === 'reply') {
          setPaneMode('editor')
          return
        }
        onSelectStep(step.id)
        if (stepReplies[step.id]) {
          setPaneMode('reply')
        } else {
          setPaneMode('editor')
        }
      }}

    Inside SortableStepRow's render, after the existing StepStatusBadge conditional block (currently lines 175-178), add the reply indicator dot:
      {stepReplies[step.id] != null && (
        <span
          className="w-1.5 h-1.5 rounded-full bg-primary shrink-0"
          aria-label="has reply"
        />
      )}

    The dot must appear after StepStatusBadge so it does not push the badge out of position. It must not appear in the DragOverlay (the overlay already renders only the step name string — no change needed there).

    Do not modify the InlineEditRow component, the rename flow, the add-step handlers, the AlertDialog, or the history/block picker integrations.
  </action>
  <verify>
    <automated>cd /Users/majesnix/gits/proto-sender && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - StepListPanel.tsx destructures stepReplies and setPaneMode from usePlanExecutionStore
    - SortableStepRowProps interface includes stepReplies: Record<string, ReplyMessage>
    - The toggle handler contains the second-click guard: `selectedStepId === step.id && paneMode === 'reply'` — grep: `grep -n "paneMode === 'reply'" src/components/plans/StepListPanel.tsx` returns a match
    - The handler calls setPaneMode('reply') when stepReplies[step.id] is truthy and setPaneMode('editor') otherwise
    - usePlanExecutionStore.getState() is used inside the onClick handler (imperative read — not a reactive selector)
    - The reply indicator dot span with aria-label="has reply" is rendered conditionally on stepReplies[step.id] != null — grep: `grep -n "has reply" src/components/plans/StepListPanel.tsx` returns a match
    - tsc exits 0 with no new type errors
  </acceptance_criteria>
  <done>StepListPanel renders a reply dot on steps with stored replies; click handler toggles paneMode based on second-click state and stepReplies presence; tsc exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Store → Display Layer | stepReplies and planReplyFeed contain data decoded in Phase 22 Rust backend; rendered by React without eval or innerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05 | Information Disclosure | PlanDetailPanel tab strip post-run | accept | Tab strip visible only to the local user on their own machine; no data leaves the process |
| T-23-06 | XSS via reply content in ResponseDecodedView | StepReplyView rendering in editor TabsContent | mitigate | Verify during read_first that ResponseDecodedView uses React text rendering (no dangerouslySetInnerHTML); if found, raise as CRITICAL blocker before proceeding |
| T-23-07 | UI Redressing / Clickjacking | Tabs layout shift on paneMode change | accept | Desktop Tauri app; no browser iframe attack surface |
| T-23-SC | Tampering | npm/cargo installs | accept | No new packages installed this phase |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0
2. `grep -n "forceMount" src/components/plans/PlanDetailPanel.tsx` — returns 1 match on editor TabsContent
3. `grep -n "activeTab !== 'editor'" src/components/plans/PlanDetailPanel.tsx` — returns 1 match (hidden className conditional)
4. `grep -n "hasRunStarted" src/components/plans/PlanDetailPanel.tsx` — definition does not reference isRunning or runningPlanId
5. `grep -n "StepReplyView\|PlanReplyFeedTab" src/components/plans/PlanDetailPanel.tsx` — both imports and render sites present
6. `grep -n "paneMode === 'reply'" src/components/plans/StepListPanel.tsx` — second-click guard present
7. `grep -n "has reply" src/components/plans/StepListPanel.tsx` — reply dot aria-label present
8. Manual behavior check: run a plan with a correlation-id step; step row shows a dot after reply arrives; clicking step shows StepReplyView in the right pane; clicking same step again returns to StepFieldEditor; Reply Feed tab shows the message with count badge
</verification>

<success_criteria>
- RESP-04: Steps with replies show decoded response inline in the right pane (StepReplyView), replacing the form, togglable back by second click
- RESP-05: [Step Editor | Reply Feed] tab strip appears after first run; Reply Feed tab shows accumulated messages in chronological accordion list; tab strip persists post-run
- Tab strip does not appear before a run starts
- Reply dot on step rows indicates stored replies
- forceMount preserves react-hook-form state during Reply Feed tab display
- tsc exits 0
</success_criteria>

<output>
Create `.planning/phases/23-response-view-inline-and-shared-feed/23-03-SUMMARY.md` when done
</output>
