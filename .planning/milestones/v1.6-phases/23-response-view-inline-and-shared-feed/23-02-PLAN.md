---
phase: "23"
plan: "02"
type: execute
wave: 2
depends_on:
  - "23-01"
files_modified:
  - src/components/plans/StepReplyView.tsx
  - src/components/plans/PlanReplyFeedTab.tsx
autonomous: true
requirements:
  - RESP-04
  - RESP-05

must_haves:
  truths:
    - "D-02: StepReplyView renders the decoded reply replacing the form — ResponseDecodedView and ResponseHexSection are composed with a step-name header; no form fields shown"
    - "D-06: PlanReplyFeedTab renders a scrollable accordion list of FeedMessage entries using the existing MessageFeedRow component; reads planReplyFeed from usePlanExecutionStore"
    - "Pitfall 6: StepReplyView shows a fallback message when decoded is null AND no error, so the pane is never blank"
    - "RESP-04 inline view component exists and composes ResponseDecodedView + ResponseHexSection"
    - "RESP-05 feed tab component exists, handles empty state, does not import MessageFeedTab"
  artifacts:
    - path: src/components/plans/StepReplyView.tsx
      provides: "Read-only reply display: step-name header + ResponseDecodedView + ResponseHexSection"
      exports:
        - StepReplyView
    - path: src/components/plans/PlanReplyFeedTab.tsx
      provides: "Reply Feed tab content: ScrollArea + Accordion + MessageFeedRow list with empty state"
      exports:
        - PlanReplyFeedTab
  key_links:
    - from: src/components/plans/StepReplyView.tsx
      to: src/components/response/ResponseDecodedView.tsx
      via: "direct import"
      pattern: "import.*ResponseDecodedView"
    - from: src/components/plans/StepReplyView.tsx
      to: src/components/response/ResponseHexSection.tsx
      via: "direct import"
      pattern: "import.*ResponseHexSection"
    - from: src/components/plans/PlanReplyFeedTab.tsx
      to: src/stores/usePlanExecutionStore.ts
      via: "usePlanExecutionStore selector"
      pattern: "usePlanExecutionStore"
    - from: src/components/plans/PlanReplyFeedTab.tsx
      to: src/components/response/MessageFeedRow.tsx
      via: "direct import"
      pattern: "import.*MessageFeedRow"
---

<objective>
Create the two new display components: StepReplyView (inline step reply panel) and PlanReplyFeedTab (shared reply feed tab content). Both are consumed by PlanDetailPanel in Plan 03 — they must be exported and ready.

Purpose: Isolates rendering logic from layout wiring. Both components are purely presentational reads from already-populated store state.
Output: StepReplyView.tsx and PlanReplyFeedTab.tsx as new files under src/components/plans/.
</objective>

<execution_context>
@/Users/majesnix/.claude/get-shit-done/workflows/execute-plan.md
@/Users/majesnix/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-response-view-inline-and-shared-feed/23-CONTEXT.md
@.planning/phases/23-response-view-inline-and-shared-feed/23-RESEARCH.md
@.planning/phases/23-response-view-inline-and-shared-feed/23-PATTERNS.md

<interfaces>
<!-- Key types and component interfaces extracted from codebase for executor reference. -->

From src/lib/types.ts:
  interface ReplyMessage {
    routingKey: string;
    contentType: string | null;
    decoded: Record<string, unknown> | null;
    decodedAs: string | null;
    hexString: string;
  }

  interface FeedMessage {
    id: string;
    routingKey: string;
    exchange: string;
    contentType: string | null;
    timestamp: number | null;
    decoded: Record<string, unknown> | null;
    hexString: string;
    error: string | null;
    decodedAs: string | null;
  }

From src/components/response/ResponseDecodedView.tsx (props contract):
  Props: { decoded: Record<string, unknown> | null, error: string | null }
  Returns null when decoded === null (early return at top of component) — caller must add fallback.

From src/components/response/ResponseHexSection.tsx (props contract):
  Props: { hexString: string, decoded: Record<string, unknown> | null }

From src/components/response/MessageFeedRow.tsx (props contract):
  Props: { message: FeedMessage }
  Requires a shadcn <Accordion> parent — component renders AccordionItem internally.

From src/stores/usePlanExecutionStore.ts (after Plan 01):
  planReplyFeed: FeedMessage[]   — selector: usePlanExecutionStore((s) => s.planReplyFeed)

shadcn ScrollArea import: import { ScrollArea } from "@/components/ui/scroll-area"
shadcn Accordion import: import { Accordion } from "@/components/ui/accordion"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StepReplyView component</name>
  <files>src/components/plans/StepReplyView.tsx</files>
  <read_first>
    src/components/response/ResponseDecodedView.tsx — verify prop names (decoded, error) and the null early-return behavior so the fallback message is placed correctly
    src/components/response/ResponseHexSection.tsx — verify prop names (hexString, decoded) and null guard behavior
    src/lib/types.ts — confirm ReplyMessage shape (already in context interfaces above)
  </read_first>
  <action>
    Create a new file src/components/plans/StepReplyView.tsx.

    Define a named interface StepReplyViewProps with two required props:
      reply: ReplyMessage
      stepName: string

    Export a named function component StepReplyView that renders:
    1. A container div with className="flex flex-col gap-4 p-4 flex-1 overflow-auto"
    2. A step-name header: div with className="text-sm text-muted-foreground" containing text "Reply from: {stepName}"
    3. ResponseDecodedView with props: decoded={reply.decoded} error={null}
    4. A fallback div (className="text-sm text-muted-foreground") with text "No decoded content available." — rendered only when reply.decoded === null (Pitfall 6: ResponseDecodedView returns null silently in this case)
    5. ResponseHexSection with props: hexString={reply.hexString} decoded={reply.decoded}

    Imports required:
      ResponseDecodedView from "@/components/response/ResponseDecodedView"
      ResponseHexSection from "@/components/response/ResponseHexSection"
      ReplyMessage type from "@/lib/types"

    Do not add forwardRef, memo, or additional state — this is a pure display component.
    Do not render any form fields, edit controls, or disabled inputs (per D-02: reply replaces form entirely).
  </action>
  <verify>
    <automated>cd /Users/majesnix/gits/proto-sender && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/plans/StepReplyView.tsx exists
    - File exports a named function StepReplyView
    - StepReplyView accepts props { reply: ReplyMessage, stepName: string }
    - Component imports ResponseDecodedView from "@/components/response/ResponseDecodedView"
    - Component imports ResponseHexSection from "@/components/response/ResponseHexSection"
    - Fallback "No decoded content available." text is rendered conditionally on reply.decoded === null (grep: `grep -n "No decoded content" src/components/plans/StepReplyView.tsx` returns a match)
    - tsc exits 0 with no new type errors
  </acceptance_criteria>
  <done>StepReplyView.tsx exists with correct props, composes ResponseDecodedView + ResponseHexSection, includes null fallback, tsc exits 0</done>
</task>

<task type="auto">
  <name>Task 2: Create PlanReplyFeedTab component</name>
  <files>src/components/plans/PlanReplyFeedTab.tsx</files>
  <read_first>
    src/components/response/MessageFeedTab.tsx — read the accordion + ScrollArea rendering pattern (structural reference only; do NOT import this component)
    src/components/response/MessageFeedRow.tsx — verify it exports MessageFeedRow and expects { message: FeedMessage }; confirm it renders an AccordionItem internally (requiring Accordion parent)
    src/stores/usePlanExecutionStore.ts — confirm planReplyFeed selector path after Plan 01 changes
  </read_first>
  <action>
    Create a new file src/components/plans/PlanReplyFeedTab.tsx.

    Export a named function component PlanReplyFeedTab with no props.

    The component reads planReplyFeed from usePlanExecutionStore using a selector:
      const planReplyFeed = usePlanExecutionStore((s) => s.planReplyFeed)

    When planReplyFeed.length === 0, return an empty-state div:
      className="p-4 text-sm text-muted-foreground"
      text: "No replies received yet — run a plan with correlation-id or first-arrival steps to see responses here."

    When planReplyFeed.length > 0, return:
      ScrollArea (className="flex-1 overflow-hidden") wrapping
        Accordion (type="single" collapsible className="w-full") wrapping
          planReplyFeed.map((msg) => <MessageFeedRow key={msg.id} message={msg} />)

    Imports required:
      usePlanExecutionStore from "@/stores/usePlanExecutionStore"
      ScrollArea from "@/components/ui/scroll-area"
      Accordion from "@/components/ui/accordion"
      MessageFeedRow from "@/components/response/MessageFeedRow"

    Do NOT import MessageFeedTab — it is coupled to useResponseStore and is a structural reference only (per PATTERNS.md key note 4).
    Do not add filter, search, or export controls — those are deferred per CONTEXT.md Deferred Ideas.
  </action>
  <verify>
    <automated>cd /Users/majesnix/gits/proto-sender && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/plans/PlanReplyFeedTab.tsx exists
    - File exports a named function PlanReplyFeedTab with no props
    - Component imports usePlanExecutionStore (grep: `grep -n "usePlanExecutionStore" src/components/plans/PlanReplyFeedTab.tsx` returns a match)
    - Component imports MessageFeedRow (grep: `grep -n "MessageFeedRow" src/components/plans/PlanReplyFeedTab.tsx` returns a match)
    - Component does NOT import MessageFeedTab or useResponseStore (grep negation: `grep "MessageFeedTab\|useResponseStore" src/components/plans/PlanReplyFeedTab.tsx` returns no output)
    - Empty state text "No replies received yet" appears in the file
    - tsc exits 0 with no new type errors
  </acceptance_criteria>
  <done>PlanReplyFeedTab.tsx exists, reads from usePlanExecutionStore.planReplyFeed, renders accordion feed or empty state, tsc exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri IPC → Store → Display | Decoded payload already type-validated in Phase 22; display components render pre-validated data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | XSS via decoded payload | StepReplyView / ResponseDecodedView | mitigate | Verify during read_first that ResponseDecodedView renders values as text content via React's default escaping — no dangerouslySetInnerHTML. If found, raise as CRITICAL blocker. |
| T-23-04 | XSS via feed entries | PlanReplyFeedTab / MessageFeedRow | accept | MessageFeedRow was audited in Phase 13; renders via React text nodes only; no HTML injection surface |
| T-23-SC | Tampering | npm installs | accept | No new packages installed this phase |
</threat_model>

<verification>
1. `ls src/components/plans/StepReplyView.tsx src/components/plans/PlanReplyFeedTab.tsx` — both files exist
2. `npx tsc --noEmit` exits 0
3. `grep -n "ResponseDecodedView\|ResponseHexSection" src/components/plans/StepReplyView.tsx` — both imports present
4. `grep -n "No decoded content available" src/components/plans/StepReplyView.tsx` — fallback text present
5. `grep -n "MessageFeedRow" src/components/plans/PlanReplyFeedTab.tsx` — import present
6. `grep "MessageFeedTab\|useResponseStore" src/components/plans/PlanReplyFeedTab.tsx` — no output (isolation confirmed)
7. `grep -n "No replies received yet" src/components/plans/PlanReplyFeedTab.tsx` — empty state present
</verification>

<success_criteria>
- StepReplyView.tsx created: composes ResponseDecodedView + ResponseHexSection, renders step-name header, has null fallback for blank decoded payload
- PlanReplyFeedTab.tsx created: reads planReplyFeed from usePlanExecutionStore, renders accordion feed or empty state, does not couple to useResponseStore or MessageFeedTab
- Both files type-check cleanly (tsc exits 0)
</success_criteria>

<output>
Create `.planning/phases/23-response-view-inline-and-shared-feed/23-02-SUMMARY.md` when done
</output>
