---
phase: "22-plan-runner-sequential-execution"
plan: "04"
type: execute
wave: 3
depends_on:
  - "22-02"
  - "22-03"
files_modified:
  - src/components/plans/PlanRunBar.tsx
  - src/components/plans/PlanDetailPanel.tsx
  - src/components/plans/StepListPanel.tsx
  - src/components/plans/StepFieldEditor.tsx
autonomous: true
requirements:
  - RUN-01
  - RUN-02
  - RUN-03
  - RUN-04
  - RUN-05
  - RUN-06

must_haves:
  truths:
    - "D-12: PlanRunBar renders in PlanDetailPanel as sticky header above the step list + editor split (RUN-01)"
    - "D-12: Run button triggers startRun; shows Stop Run while running; re-enables with Re-run Plan after completion (RUN-01, RUN-02)"
    - "D-08: Stop on Error toggle is present in PlanRunBar and persists via updatePlan (RUN-04)"
    - "D-13: Run button disabled when plan has 0 steps or no active connection profile, with Tooltip explaining why (UI-SPEC)"
    - "D-11: Run summary line '✓ N/M succeeded' or '✗ N/M succeeded' appears in PlanRunBar after run completes (RUN-05)"
    - "D-09: StepStatusBadge is visible on each step row in StepListPanel during and after a run (RUN-03)"
    - "D-10: Active step row highlighted with bg-accent text-accent-foreground during run (UI-SPEC)"
    - "D-09: Phase 21 two-pane layout preserved during run — no layout switch (D-09)"
    - "D-09: All StepFieldEditor inputs are disabled while runningPlanId !== null (isRunning=true); no visual change needed beyond shadcn disabled state"
    - "D-10: Active step row scrolls into view when activeStepId changes (scrollIntoView smooth/nearest via ref on SortableStepRow)"
    - "D-10: StepFieldEditor auto-switches to show active step fields during run — selectedStepId in PlanDetailPanel syncs to activeStepId"
  artifacts:
    - path: "src/components/plans/PlanRunBar.tsx"
      provides: "Run/Stop/Re-run controls + stop_on_error toggle + summary"
      contains: "PlanRunBar"
    - path: "src/components/plans/PlanDetailPanel.tsx"
      provides: "PlanRunBar mounted above step list/editor split; activeStepId sync to selectedStepId"
      contains: "PlanRunBar"
    - path: "src/components/plans/StepListPanel.tsx"
      provides: "StepStatusBadge per row + active step highlight + scrollIntoView on active step"
      contains: "StepStatusBadge"
    - path: "src/components/plans/StepFieldEditor.tsx"
      provides: "disabled prop threaded to all inputs — read-only during run (D-09)"
      contains: "disabled"
  key_links:
    - from: "src/components/plans/PlanRunBar.tsx"
      to: "src/hooks/usePlanRunner.ts"
      via: "startRun, stopRun"
      pattern: "usePlanRunner"
    - from: "src/components/plans/PlanRunBar.tsx"
      to: "src/stores/usePlanStore.ts"
      via: "updatePlan for stop_on_error toggle"
      pattern: "updatePlan"
    - from: "src/components/plans/StepListPanel.tsx"
      to: "src/stores/usePlanExecutionStore.ts"
      via: "stepStatuses[step.id], activeStepId"
      pattern: "usePlanExecutionStore"
    - from: "src/components/plans/PlanDetailPanel.tsx"
      to: "src/stores/usePlanExecutionStore.ts"
      via: "activeStepId → selectedStepId sync during run"
      pattern: "activeStepId"
---

<objective>
Assemble the plan runner UI: PlanRunBar (new), PlanDetailPanel (add bar above split +
activeStepId→selectedStepId sync), StepListPanel (add StepStatusBadge per row + active
step highlight + scrollIntoView ref + disabled prop to StepFieldEditor during run),
StepFieldEditor (add disabled prop and thread to all inputs).

Purpose: Final wave — depends on Rust backend (22-02) for execute_step/cancel_plan_run
and on store + hook + badge (22-03). After this plan, Phase 22 is fully executable.

Output: Working plan runner UI. User can click Run Plan, watch per-step status badges
update live, click Stop Run at any time, and see a run summary. Editor inputs are
disabled during run and the editor auto-switches to the active step.
</objective>

<execution_context>
@/Users/majesnix/.claude/get-shit-done/workflows/execute-plan.md
@/Users/majesnix/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/majesnix/gits/proto-sender/.planning/PROJECT.md
@/Users/majesnix/gits/proto-sender/.planning/ROADMAP.md
@/Users/majesnix/gits/proto-sender/.planning/STATE.md
@/Users/majesnix/gits/proto-sender/.planning/phases/22-plan-runner-sequential-execution/22-CONTEXT.md
@/Users/majesnix/gits/proto-sender/.planning/phases/22-plan-runner-sequential-execution/22-UI-SPEC.md
@/Users/majesnix/gits/proto-sender/.planning/phases/22-plan-runner-sequential-execution/22-03-SUMMARY.md
@/Users/majesnix/gits/proto-sender/.planning/phases/22-plan-runner-sequential-execution/22-02-SUMMARY.md
</context>

<interfaces>
<!-- Contracts from 22-01 through 22-03. Do NOT re-read source files. -->

From 22-03 (usePlanRunner hook):
```typescript
// src/hooks/usePlanRunner.ts
export function usePlanRunner(): {
  startRun: (plan: Plan) => Promise<void>;
  stopRun: () => Promise<void>;
  isRunning: boolean;
}
```

From 22-03 (usePlanExecutionStore):
```typescript
// src/stores/usePlanExecutionStore.ts
interface PlanExecutionState {
  runningPlanId: string | null;
  stepStatuses: Record<string, StepStatus>;
  activeStepId: string | null;
  isCancelling: boolean;
  summary: { succeeded: number; total: number } | null;
}
```

From 22-01 (usePlanStore.updatePlan):
```typescript
// src/stores/usePlanStore.ts
updatePlan(id: string, partial: Partial<Plan>): Promise<void>;
```

From 22-03 (StepStatusBadge):
```typescript
// src/components/plans/StepStatusBadge.tsx
export function StepStatusBadge({ status }: { status: StepStatus }): JSX.Element
```

From existing PlanDetailPanel.tsx (current layout — for integration context):
// Returns:
//   <div className="flex flex-1 min-h-0 min-w-0">
//     <DndContext ...>
//       <StepListPanel ... />
//       <DragOverlay ... />
//     </DndContext>
//     <StepFieldEditor ... />
//   </div>
// The outer div must become a flex col to host PlanRunBar above the horizontal split.

From 22-UI-SPEC.md (PlanRunBar spec):
- Container: flex items-center gap-4 bg-card border-b border-border px-4 py-2 shrink-0
- Plan name: text-sm font-medium truncate flex-1 (truncated, takes remaining space)
- Run idle: Button variant="default" size="sm" + Play icon 14px + "Run Plan"
- Run running: Button variant="destructive" size="sm" + Square icon 14px + "Stop Run"
- Run disabled: wrapped in <span> inside <Tooltip> (not on Button directly — shadcn Tooltip bug with disabled buttons)
- Disable conditions: plan.steps.length === 0, OR no active connection profile
- Tooltip text when no steps: "Add at least one step to run this plan"
- Tooltip text when no profile: "Connect to a profile first"
- Stop on error toggle: Switch + Label "Stop on error" (left of run button)
- Summary success line: text-emerald-700 dark:text-emerald-400 font-semibold "✓ {N}/{M} succeeded"
- Summary failure line: text-destructive font-semibold "✗ {N}/{M} succeeded"
- Re-run button: variant="outline" size="sm" "Re-run Plan" (appears after run completes, next to summary)
- Success = succeeded === total; failure = succeeded < total

From 22-UI-SPEC.md (StepFieldEditor disabled during run):
- All inputs in StepFieldEditor receive disabled prop while runningPlanId !== null
- No special visual indicator needed beyond shadcn grayed input (standard disabled state)
- StepFieldEditor auto-switches to show active step during run (D-10)

From existing StepListPanel.tsx (SortableStepRow usage, for badge integration):
// Each step renders as <SortableStepRow key={step.id} step={step} isSelected=... />
// Must add StepStatusBadge inline in each row and active-step highlight on the row
// Active step: bg-accent text-accent-foreground (same as isSelected highlight)
// scrollIntoView: attach a ref to the SortableStepRow container; call
//   rowRef.current?.scrollIntoView({ behavior: 'smooth', block: 'nearest' })
//   inside a useEffect watching isActiveStep
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: PlanRunBar component</name>
  <files>src/components/plans/PlanRunBar.tsx</files>
  <action>
Create src/components/plans/PlanRunBar.tsx.

Props:
```typescript
interface PlanRunBarProps {
  plan: Plan;
}
```

Imports needed: usePlanRunner (22-03), usePlanExecutionStore (22-03), usePlanStore
(for updatePlan), useConnectionStore (for active profile — check what selector provides
the active profile name; likely selectActiveProfile or activeProfile), Button, Switch,
Label, Tooltip, TooltipContent, TooltipTrigger, TooltipProvider from shadcn/ui,
Play + Square from lucide-react, Plan + StepStatus from types.

Implementation:

1. Derive state:
   - const { startRun, stopRun, isRunning } = usePlanRunner()
   - const { summary, isCancelling } = usePlanExecutionStore()
   - const { updatePlan } = usePlanStore()
   - Read active profile: const activeProfile = useConnectionStore(s => s.activeProfile)
     or equivalent — check the existing store for the correct selector name
   - const stopOnError = plan.stop_on_error ?? true
   - const canRun = plan.steps.length > 0 && activeProfile !== null && !isRunning

2. Disable reason (for Tooltip):
   - if (plan.steps.length === 0): "Add at least one step to run this plan"
   - else if (!activeProfile): "Connect to a profile first"
   - else: null (no tooltip needed)

3. Run button rendering (per UI-SPEC):
   If isRunning: variant="destructive" + Square icon + "Stop Run" — onClick calls stopRun()
   Else if !isRunning and summary !== null: show Re-run button instead (variant="outline" "Re-run Plan")
     PLUS the summary line (same row — summary left, re-run right)
   Else (idle, no summary): variant="default" + Play icon + "Run Plan"

   When idle and canRun is false: wrap the button in Tooltip (per PublishBar pattern
   for disabled buttons — Tooltip wraps a <span> which wraps the Button):
   <TooltipProvider>
     <Tooltip>
       <TooltipTrigger asChild>
         <span>
           <Button disabled ...>Run Plan</Button>
         </span>
       </TooltipTrigger>
       <TooltipContent>{disableReason}</TooltipContent>
     </Tooltip>
   </TooltipProvider>
   When canRun is true: no Tooltip wrapper needed.

4. Stop on error toggle:
   <div className="flex items-center gap-2">
     <Switch
       id="stop-on-error"
       checked={stopOnError}
       disabled={isRunning}
       onCheckedChange={(checked) => updatePlan(plan.id, { stop_on_error: checked }).catch(console.error)}
     />
     <Label htmlFor="stop-on-error" className="text-sm">Stop on error</Label>
   </div>

5. Summary line (shown when summary !== null AND !isRunning):
   - success: <span className="text-emerald-700 dark:text-emerald-400 font-semibold text-sm">✓ {summary.succeeded}/{summary.total} succeeded</span>
   - failure: <span className="text-destructive font-semibold text-sm">✗ {summary.succeeded}/{summary.total} succeeded</span>
   success condition: summary.succeeded === summary.total

6. Container layout per UI-SPEC:
   <div className="flex items-center gap-4 bg-card border-b border-border px-4 py-2 shrink-0">
     <span className="text-sm font-medium truncate flex-1">{plan.name}</span>
     {/* stop on error toggle */}
     {/* summary line when available */}
     {/* run/stop/re-run button */}
   </div>

Note: isCancelling state is managed by usePlanRunner — PlanRunBar does not need to
read it directly. The Stop Run button calls stopRun() which sets isCancelling
internally. The button remains "Stop Run" until isRunning becomes false.
  </action>
  <verify>
    <automated>cd /Users/majesnix/gits/proto-sender && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    TypeScript compiles. PlanRunBar.tsx exists.
    grep -n "PlanRunBar\|startRun\|stopRun\|updatePlan\|stop_on_error" src/components/plans/PlanRunBar.tsx
    shows all key identifiers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire PlanRunBar into PlanDetailPanel + add StepStatusBadge, auto-scroll, disabled state to StepListPanel + StepFieldEditor</name>
  <files>src/components/plans/PlanDetailPanel.tsx, src/components/plans/StepListPanel.tsx, src/components/plans/StepFieldEditor.tsx</files>
  <action>
MODIFY src/components/plans/PlanDetailPanel.tsx:

1. Import PlanRunBar from "./PlanRunBar"
2. Import usePlanExecutionStore from "@/stores/usePlanExecutionStore"
3. Change the return layout from the current horizontal-only flex to a flex-col wrapper
   so PlanRunBar sits above the step list + editor split:

Current structure:
```
<div className="flex flex-1 min-h-0 min-w-0">
  <DndContext>...</DndContext>
  <StepFieldEditor ... />
</div>
```

New structure:
```
<div className="flex flex-1 flex-col min-h-0 min-w-0">
  <PlanRunBar plan={selectedPlan} />
  <div className="flex flex-1 min-h-0 min-w-0">
    <DndContext>...</DndContext>
    <StepFieldEditor disabled={isRunning} selectedStepId={effectiveSelectedStepId} ... />
  </div>
</div>
```

4. D-10 — Editor auto-switch to active step during run (per D-10, UI-SPEC):
   Read activeStepId from usePlanExecutionStore:
   const { activeStepId, runningPlanId } = usePlanExecutionStore()
   const isRunning = runningPlanId !== null

   Derive the effective selected step: during a run, override selectedStepId with
   activeStepId so the editor auto-switches to show the active step's fields:
   const effectiveSelectedStepId = isRunning && activeStepId !== null
     ? activeStepId
     : selectedStepId

   Pass effectiveSelectedStepId to StepFieldEditor (and to StepListPanel for the
   isSelected highlight) instead of selectedStepId directly.

5. D-09 — StepFieldEditor inputs disabled during run:
   Pass disabled={isRunning} to StepFieldEditor. The disabled prop is added to
   StepFieldEditor in this same task (see StepFieldEditor section below).

Keep all existing state (selectedStepId, activeDragId), sensors, drag handlers, and
imports intact. Add only the new imports and the effectiveSelectedStepId derivation.

---

MODIFY src/components/plans/StepListPanel.tsx:

1. Import StepStatusBadge from "./StepStatusBadge"
2. Import usePlanExecutionStore from "@/stores/usePlanExecutionStore"
3. Import useRef, useEffect from react (needed for scrollIntoView)

4. Inside the StepListPanel function body, read execution state:
   const { stepStatuses, activeStepId } = usePlanExecutionStore()

5. Pass stepStatuses and activeStepId down to SortableStepRow. Add two props to
   SortableStepRow's interface:
   - stepStatus: StepStatus | undefined  (undefined when not running)
   - isActiveStep: boolean

6. In the SortableStepRow component (inner function), implement:

   a. D-10 — Auto-scroll to active step:
      Add a ref to the row's container element:
        const rowRef = useRef<HTMLDivElement>(null)
      Add a useEffect watching isActiveStep:
        useEffect(() => {
          if (isActiveStep) {
            rowRef.current?.scrollIntoView({ behavior: 'smooth', block: 'nearest' })
          }
        }, [isActiveStep])
      Attach ref to the outermost row container div: ref={rowRef}

   b. Active step highlight: when isActiveStep is true, apply bg-accent text-accent-foreground
      to the row container (same className pattern as isSelected). The row can have both
      isSelected and isActiveStep true simultaneously — apply both classes.

   c. StepStatusBadge: render <StepStatusBadge status={stepStatus} /> only when stepStatus
      is defined (i.e. execution is in progress or has completed):
        {stepStatus !== undefined && <StepStatusBadge status={stepStatus} />}
      Place the badge at the right end of the step row, before or instead of the
      context menu trigger area. Find the correct slot in the existing SortableStepRow
      JSX — add it inside the row container, right-aligned.

7. Update the call site inside StepListPanel's map to pass the new props:
   stepStatus={stepStatuses[step.id]}
   isActiveStep={activeStepId === step.id}

Read SortableStepRow's interface definition at the top of StepListPanel.tsx and add
the two new props there before implementing. The interface is around line 109–130.

Do NOT change StepListPanel's own props interface (plan, selectedStepId, onSelectStep,
activeDragId) — these come from PlanDetailPanel and must remain stable.

---

MODIFY src/components/plans/StepFieldEditor.tsx:

Read StepFieldEditor.tsx ONCE before editing to understand its props interface and
the inputs/selects/textareas it renders. Then:

1. Add disabled?: boolean to the StepFieldEditorProps interface (or equivalent props
   type at the top of the file).

2. Thread disabled={disabled} down to every input, select, and textarea element inside
   the component. Do NOT change the component's internal logic or layout — only add
   the disabled prop to each field element.

3. If StepFieldEditor contains sub-components (e.g., form field wrappers), pass
   disabled through to their underlying HTML input/select/textarea elements.

The only visual change is the shadcn grayed-out state for disabled inputs — no
additional styling needed.
  </action>
  <verify>
    <automated>cd /Users/majesnix/gits/proto-sender && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    TypeScript compiles with no errors. PlanDetailPanel renders PlanRunBar above the
    split. StepListPanel rows show StepStatusBadge when stepStatus is defined.
    StepFieldEditor accepts and threads disabled prop.
    grep -n "PlanRunBar" src/components/plans/PlanDetailPanel.tsx → import + usage
    grep -n "StepStatusBadge\|stepStatuses\|activeStepId" src/components/plans/StepListPanel.tsx → all 3
    grep -n "scrollIntoView" src/components/plans/StepListPanel.tsx → present in useEffect
    grep -n "disabled" src/components/plans/PlanDetailPanel.tsx → disabled={isRunning} wired to StepFieldEditor
    grep -n "disabled" src/components/plans/StepFieldEditor.tsx → in props interface + on field elements
    grep -n "effectiveSelectedStepId\|activeStepId" src/components/plans/PlanDetailPanel.tsx → both present
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI toggle → updatePlan | stop_on_error boolean; boolean-typed, not user text input |
| summary.succeeded display | integer derived from Rust command results; no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-10 | Tampering | PlanRunBar double-submit | mitigate | isRunning=true disables Run button; Button is disabled during run — second click impossible |
| T-22-11 | Denial of Service | Stop button during cancel | accept | stopRun() calls cancelPlanRun() which is idempotent; double-clicking Stop does not cause issues |
| T-22-12 | Information Disclosure | plan.name truncation in PlanRunBar | accept | Plan name is user data, displayed to the same user who created it |
| T-22-SC | Tampering | npm installs | mitigate | No new npm packages; all components from existing shadcn/ui + lucide-react |
</threat_model>

<verification>
- npx tsc --noEmit passes after Task 1 (PlanRunBar)
- npx tsc --noEmit passes after Task 2 (PlanDetailPanel + StepListPanel + StepFieldEditor)
- grep -n "PlanRunBar" src/components/plans/PlanDetailPanel.tsx → import + usage in return JSX
- grep -n "flex-col" src/components/plans/PlanDetailPanel.tsx → outer wrapper is flex-col
- grep -n "StepStatusBadge" src/components/plans/StepListPanel.tsx → import + conditional render
- grep -n "stop_on_error\|updatePlan" src/components/plans/PlanRunBar.tsx → both present
- grep -n "scrollIntoView" src/components/plans/StepListPanel.tsx → present in useEffect on isActiveStep
- grep -n "disabled" src/components/plans/PlanDetailPanel.tsx → disabled={isRunning} passed to StepFieldEditor
- grep -n "disabled" src/components/plans/StepFieldEditor.tsx → in props + on input elements
- grep -n "effectiveSelectedStepId\|activeStepId" src/components/plans/PlanDetailPanel.tsx → editor auto-switch wired
</verification>

<success_criteria>
- PlanRunBar renders above the step list/editor split in PlanDetailPanel
- Run Plan button enables when plan has steps and active profile
- Clicking Run Plan: button changes to "Stop Run", step badges update as each step executes
- Clicking Stop Run mid-run: cancels gracefully, summary shows correct succeeded count
- Summary line appears below/next to Re-run Plan button after run completes
- Stop on error toggle persists via updatePlan (check store after toggle)
- StepStatusBadge visible on each row during and after run
- Active step row highlighted with bg-accent during run
- Active step row scrolls into view automatically as execution advances
- StepFieldEditor inputs are disabled (grayed) while a run is in progress
- StepFieldEditor auto-switches to display the active step's fields during run
- Zero TypeScript compiler errors
</success_criteria>

<output>
Create `/Users/majesnix/gits/proto-sender/.planning/phases/22-plan-runner-sequential-execution/22-04-SUMMARY.md` when done.
</output>
