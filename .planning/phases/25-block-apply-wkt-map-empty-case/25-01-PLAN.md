---
phase: 25-block-apply-wkt-map-empty-case
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/blockApply.ts
  - src/lib/blockApply.test.ts
autonomous: true
requirements:
  - BLK-EXT-07

must_haves:
  truths:
    - "buildApplyPlan returns toApply items for non-dirty scalar, enum, and well_known fields"
    - "buildApplyPlan skips well_known fields that are dirty (D-06)"
    - "buildApplyPlan returns a toApply map item when the map field is currently empty"
    - "buildApplyPlan skips map fields that are currently non-empty (Phase 26 handles conflict)"
    - "buildApplyPlan silently skips block keys that do not match any eligible field"
    - "buildApplyPlan silently skips 'message' kind fields (D-02 + BLK-EXT-FUTURE-02)"
    - "conflicts is always [] in Phase 25 output"
    - "ApplyBlockRef type is exported from blockApply.ts for use in ProtoFormRenderer and FormPanel"
  artifacts:
    - path: "src/lib/blockApply.ts"
      provides: "buildApplyPlan pure function; ApplyPlan, ApplyItem, ConflictItem, ApplyItemKind, ApplyBlockRef types"
      exports: ["buildApplyPlan", "ApplyPlan", "ApplyItem", "ConflictItem", "ApplyItemKind", "ApplyBlockRef"]
    - path: "src/lib/blockApply.test.ts"
      provides: "Unit test coverage for all D-04 cases plus message-kind and non-empty-map cases"
      contains: "describe(\"buildApplyPlan\""
  key_links:
    - from: "src/lib/blockApply.ts"
      to: "src/lib/types.ts"
      via: "import type { FieldSchema }"
      pattern: "import type.*FieldSchema.*from.*@/lib/types"
---

<objective>
Create the pure `buildApplyPlan` function in `src/lib/blockApply.ts` and full test coverage in `src/lib/blockApply.test.ts` using TDD red-green-refactor cycles.

Purpose: Establish the plan-phase of the two-phase block-apply architecture (D-01, D-03). The pure function is the prerequisite for the component wiring in plan 25-02.

Output:
- `src/lib/blockApply.ts` — exported types (ApplyPlan, ApplyItem, ConflictItem, ApplyItemKind, ApplyBlockRef) and the `buildApplyPlan` pure function
- `src/lib/blockApply.test.ts` — complete unit tests (all D-04 cases plus message-kind-skip and non-empty-map-skip cases)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-block-apply-wkt-map-empty-case/25-CONTEXT.md
@.planning/phases/25-block-apply-wkt-map-empty-case/25-RESEARCH.md
@.planning/phases/25-block-apply-wkt-map-empty-case/25-PATTERNS.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: TDD — blockApply.ts types and buildApplyPlan pure function</name>
  <files>src/lib/blockApply.ts, src/lib/blockApply.test.ts</files>
  <read_first>
    - src/lib/types.ts — FieldSchema, FieldKind type definitions; ELIGIBLE_KINDS must use FieldSchema['kind']['type'] values
    - src/components/history/historyHelpers.ts — import style (type-only imports at top), JSDoc pattern, pure function shape, no try/catch
    - src/components/history/historyHelpers.test.ts — test file structure: makeEntry helper, describe/it/expect from vitest, AAA pattern with comments, makeField factory pattern
    - src/components/form/ProtoFormRenderer.tsx lines 149-180 — existing applyBlockRef implementation being replaced; shows eligibleFields set logic and shouldDirty: false comment
    - .planning/phases/25-block-apply-wkt-map-empty-case/25-CONTEXT.md — D-01 through D-06 locked decisions; D-02 ApplyPlan shape, D-03 function signature
    - .planning/phases/25-block-apply-wkt-map-empty-case/25-RESEARCH.md — PATTERNS section 3 (buildApplyPlan code example), Pitfall 4 (dirtyFields top-level access), OQ-1 and OQ-4 resolution
  </read_first>
  <behavior>
    RED phase — write these test cases first (all must fail before any implementation):
    - "fills scalar field when not dirty": fields=[{name:'qty', kind:{type:'scalar',scalar:'int32'}}], dirtyFields={}, blockValues={qty:5} → toApply=[{fieldName:'qty',value:5,kind:'scalar'}], conflicts=[]
    - "fills enum field when not dirty": fields=[{name:'status',kind:{type:'enum',values:[{name:'OK',number:0}]}}], dirtyFields={}, blockValues={status:1} → toApply=[{fieldName:'status',value:1,kind:'enum'}]
    - "fills well_known field when not dirty (D-06)": fields=[{name:'ts',kind:{type:'well_known',wkt:'Timestamp'}}], formValues={ts:null}, dirtyFields={}, blockValues={ts:'2026-01-01T00:00:00Z'} → toApply=[{fieldName:'ts',value:'2026-01-01T00:00:00Z',kind:'well_known'}]
    - "skips well_known field when dirty (D-06)": same setup but dirtyFields={ts:true} → toApply=[]
    - "fills map field when empty (BLK-EXT-02)": fields=[{name:'labels',kind:{type:'map',key_type:'string',value_kind:{type:'scalar',scalar:'string'}}}], formValues={labels:[]}, dirtyFields={}, blockValues={labels:[{key:'env',value:'prod'}]} → toApply=[{fieldName:'labels',value:[{key:'env',value:'prod'}],kind:'map'}]
    - "skips map field when non-empty (Phase 26 conflict path)": same but formValues={labels:[{key:'existing',value:'v'}]} → toApply=[], conflicts=[]
    - "skips unknown block key silently": fields=[{name:'name',kind:{type:'scalar',scalar:'string'}}], blockValues={unknownField:'x'} → toApply=[], conflicts=[]
    - "skips message kind field (D-02 + BLK-EXT-FUTURE-02)": fields=[{name:'nested',kind:{type:'message',full_name:'com.Nested'}}], blockValues={nested:{foo:'bar'}} → toApply=[], conflicts=[]
    - "conflicts is always empty array in Phase 25": any valid input → conflicts=[]

    GREEN phase — implement buildApplyPlan to pass all tests.

    REFACTOR phase — ensure no code smell: no magic numbers, types exported cleanly, function body under 40 lines.
  </behavior>
  <action>
    Create src/lib/blockApply.test.ts first (RED):
    - Import { describe, it, expect } from "vitest"
    - Import { buildApplyPlan } from "@/lib/blockApply"
    - Import type { FieldSchema } from "@/lib/types"
    - Create makeField(overrides: Partial&lt;FieldSchema&gt; = {}): FieldSchema factory with sensible defaults; create makeScalarField, makeWktField, makeMapField, makeEnumField, makeMessageField shorthands following historyHelpers.test.ts makeEntry pattern
    - Write all 9 test cases listed in behavior block using AAA pattern with // Arrange / // Act / // Assert comments
    - Run `npx vitest run src/lib/blockApply.test.ts` — ALL tests must FAIL (file does not exist yet)

    Create src/lib/blockApply.ts (GREEN):
    - Top-of-file: `import type { FieldSchema } from "@/lib/types";` (type-only import, @/ alias, no other imports)
    - Export named types in this order: ApplyItemKind, ApplyItem, ConflictItem, ApplyPlan, ApplyBlockRef
      - ApplyItemKind = 'scalar' | 'enum' | 'well_known' | 'map'
      - ApplyItem = { fieldName: string; value: unknown; kind: ApplyItemKind }
      - ConflictItem = { fieldName: string; blockValue: unknown; currentValue: unknown; kind: ApplyItemKind }
      - ApplyPlan = { toApply: ApplyItem[]; conflicts: ConflictItem[] }
      - ApplyBlockRef = { buildPlan: (blockValues: Record&lt;string, unknown&gt;) =&gt; ApplyPlan; commitApply: (plan: ApplyPlan) =&gt; void }
    - ELIGIBLE_KINDS as ReadonlySet: new Set(['scalar', 'enum', 'well_known', 'map'])
    - JSDoc block above buildApplyPlan explaining purpose, per historyHelpers.ts JSDoc pattern
    - Export function buildApplyPlan(fields: FieldSchema[], formValues: Record&lt;string, unknown&gt;, dirtyFields: Partial&lt;Record&lt;string, unknown&gt;&gt;, blockValues: Record&lt;string, unknown&gt;): ApplyPlan
    - Eligibility: build Map from fields filtered by ELIGIBLE_KINDS (per D-02 — 'message' kind is intentionally excluded per BLK-EXT-FUTURE-02 deferral; previously shallow setValue on message objects silently routed through old code; Phase 25 correctly routes these to the skipped-toast path in FormPanel)
    - For each blockValues entry: skip if field not in eligible map, skip if dirtyFields[key] truthy, skip map if formValues[key] is Array with length > 0 (Phase 26 conflict path), else push toApply item with correct kind
    - Return { toApply, conflicts: [] } — conflicts always empty in Phase 25
    - Run `npx vitest run src/lib/blockApply.test.ts` — ALL tests must PASS
  </action>
  <verify>
    <automated>cd /Users/majesnix/gits/proto-sender && npx vitest run src/lib/blockApply.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/blockApply.ts exists and exports: buildApplyPlan, ApplyPlan, ApplyItem, ConflictItem, ApplyItemKind, ApplyBlockRef
    - src/lib/blockApply.test.ts exists with at least 9 test cases in describe("buildApplyPlan") block
    - `npx vitest run src/lib/blockApply.test.ts` exits 0 with all tests passing
    - buildApplyPlan has explicit parameter types and return type (no implicit any)
    - No imports from React or react-hook-form in blockApply.ts (pure function — no side effects)
    - ApplyBlockRef type is exported (consumed by ProtoFormRenderer and FormPanel in plan 25-02)
    - TypeScript compilation: `npx tsc --noEmit` exits 0 for the new file
  </acceptance_criteria>
  <done>All 9 test cases pass; blockApply.ts TypeScript-clean; ApplyBlockRef exported for downstream consumers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Block JSON → buildApplyPlan | Block content originates from user-created block library (trusted local data); parsed in FormPanel before being passed as blockValues — no network source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | buildApplyPlan inputs (blockValues) | accept | Block values come from user's own saved block library, parsed through JSON.parse guard in FormPanel.onDragEnd (`typeof parsed !== 'object'`); no external or untrusted data path |

Note: T-25-SC not applicable — no new packages installed in this phase.
</threat_model>

<verification>
- `npx vitest run src/lib/blockApply.test.ts` — all cases pass
- `npx tsc --noEmit` — no TypeScript errors
- grep -c "export" src/lib/blockApply.ts shows at least 6 exports (ApplyItemKind, ApplyItem, ConflictItem, ApplyPlan, ApplyBlockRef, buildApplyPlan)
</verification>

<success_criteria>
- blockApply.ts exists with pure buildApplyPlan function and all 5 exported types
- 9+ test cases pass covering all D-04 cases plus message-kind-skip and non-empty-map-skip
- No TypeScript errors
- ApplyBlockRef type available for import in plan 25-02
</success_criteria>

<output>
Create `.planning/phases/25-block-apply-wkt-map-empty-case/25-01-SUMMARY.md` when done
</output>
