---
phase: "23"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/usePlanExecutionStore.ts
  - src/hooks/usePlanRunner.ts
autonomous: true
requirements:
  - RESP-04
  - RESP-05

must_haves:
  truths:
    - "D-09: usePlanExecutionStore has stepReplies, planReplyFeed, and paneMode fields with correct initial values in INITIAL_STATE"
    - "D-11: planReplyFeed FIFO cap is 500 entries — appendReplyFeedEntry prepends the new entry and slices to 500"
    - "D-13: setStepReply action stores a ReplyMessage keyed by step ID using an immutable spread"
    - "D-04: usePlanRunner calls setPaneMode('editor') before each executeStep call and setPaneMode('reply') inside the result.reply !== null branch"
    - "D-12: appendReplyFeedEntry is called only inside the result.reply !== null branch — no-wait steps produce zero feed entries"
    - "D-10: only correlation-id and first-arrival steps with a non-null reply contribute entries to planReplyFeed"
    - "Pitfall 3: setRunning action resets stepReplies, planReplyFeed, and paneMode inline alongside existing summary and isCancelling resets"
  artifacts:
    - path: src/stores/usePlanExecutionStore.ts
      provides: "Extended store with stepReplies, planReplyFeed, paneMode + four new actions"
      exports:
        - usePlanExecutionStore
        - useRawPlanExecutionStore
    - path: src/hooks/usePlanRunner.ts
      provides: "Runner loop extended with setStepReply, setPaneMode, appendReplyFeedEntry calls"
  key_links:
    - from: src/hooks/usePlanRunner.ts
      to: src/stores/usePlanExecutionStore.ts
      via: "destructured setStepReply, setPaneMode, appendReplyFeedEntry actions"
      pattern: "setStepReply|setPaneMode|appendReplyFeedEntry"
---

<objective>
Extend the ephemeral execution store with the three new fields (stepReplies, planReplyFeed, paneMode) and wire the runner hook to populate them after each step that returns a non-null reply.

Purpose: Provides the data layer that the display components in Plans 02 and 03 consume. All decisions D-04, D-09 through D-13 land here.
Output: usePlanExecutionStore with new state and actions; usePlanRunner dispatching setStepReply, setPaneMode, and appendReplyFeedEntry after reply-bearing steps.
</objective>

<execution_context>
@/Users/majesnix/.claude/get-shit-done/workflows/execute-plan.md
@/Users/majesnix/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-response-view-inline-and-shared-feed/23-CONTEXT.md
@.planning/phases/23-response-view-inline-and-shared-feed/23-RESEARCH.md
@.planning/phases/23-response-view-inline-and-shared-feed/23-PATTERNS.md

<interfaces>
<!-- Key types the executor needs. Extracted from src/lib/types.ts and current store. -->

From src/lib/types.ts:
  interface ReplyMessage {
    routingKey: string;
    contentType: string | null;
    decoded: Record<string, unknown> | null;
    decodedAs: string | null;
    hexString: string;
  }

  interface FeedMessage {
    id: string;                      // crypto.randomUUID() at append time
    routingKey: string;
    exchange: string;
    contentType: string | null;
    timestamp: number | null;
    decoded: Record<string, unknown> | null;
    hexString: string;
    error: string | null;
    decodedAs: string | null;
  }

  interface StepResult {
    status: 'done' | 'error';
    reply: ReplyMessage | null;
    error: string | null;
  }

From src/stores/usePlanExecutionStore.ts (current state — all fields to be preserved):
  INITIAL_STATE = {
    runningPlanId: null,
    stepStatuses: {},
    activeStepId: null,
    isCancelling: false,
    summary: null,
    isRunning: false,
  }

  setRunning(planId, stepIds) — sets pending status for all steps, resets summary/isCancelling/isRunning inline
  clearRun() — calls set({ ...INITIAL_STATE })

From src/hooks/usePlanRunner.ts (runner loop structure):
  const result = await executeStep(activeProfileName, step);   // line 67
  if (result.status === 'done') {
    setStepStatus(step.id, 'done');   // line 70
    succeeded++;                       // line 71
  }
  // Phase 23 additions insert after setStepStatus, before succeeded++
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend usePlanExecutionStore with stepReplies, planReplyFeed, and paneMode</name>
  <files>src/stores/usePlanExecutionStore.ts</files>
  <read_first>
    src/stores/usePlanExecutionStore.ts — read the full file to see INITIAL_STATE, setRunning body, clearRun body, and existing action patterns before editing
    src/lib/types.ts — confirm ReplyMessage and FeedMessage type shapes (already in context above)
  </read_first>
  <behavior>
    - appendReplyFeedEntry with a single entry on an empty feed produces planReplyFeed of length 1 with that entry at index 0
    - appendReplyFeedEntry called 501 times produces planReplyFeed of length 500 (FIFO cap; oldest entries are dropped)
    - appendReplyFeedEntry uses immutable spread: state.planReplyFeed is not mutated in-place
    - setStepReply('step-1', reply) produces stepReplies containing { 'step-1': reply }, leaving other keys unchanged
    - setStepReply called twice for different stepIds accumulates both entries (immutable record spread)
    - setPaneMode('reply') sets paneMode to 'reply'; setPaneMode('editor') sets it back to 'editor'
    - setRunning resets stepReplies to {}, planReplyFeed to [], paneMode to 'editor' alongside existing resets
    - clearRun resets all new fields via INITIAL_STATE spread (no separate reset needed if INITIAL_STATE is updated)
    - INITIAL_STATE.stepReplies = {}, INITIAL_STATE.planReplyFeed = [], INITIAL_STATE.paneMode = 'editor'
  </behavior>
  <action>
    Add to the import at line 2: import type { StepStatus, ReplyMessage, FeedMessage } from "../lib/types";

    Extend PlanExecutionState interface with three new fields:
      stepReplies: Record<string, ReplyMessage>
      planReplyFeed: FeedMessage[]
      paneMode: 'editor' | 'reply'

    Extend PlanExecutionActions interface with four new actions:
      setStepReply: (stepId: string, reply: ReplyMessage) => void
      appendReplyFeedEntry: (entry: FeedMessage) => void
      setPaneMode: (mode: 'editor' | 'reply') => void

    Update INITIAL_STATE to include:
      stepReplies: {} as Record<string, ReplyMessage>
      planReplyFeed: [] as FeedMessage[]
      paneMode: 'editor' as const

    Update the setRunning action body to reset the three new fields inline alongside the existing summary: null reset — do NOT delegate to clearRun (per D-13 pattern and Pitfall 3):
      stepReplies: {}
      planReplyFeed: []
      paneMode: 'editor'

    clearRun already calls set({ ...INITIAL_STATE }) so it picks up the new fields automatically — no change needed there.

    Implement the three new actions using immutable patterns (per coding-style.md CRITICAL rule):

    setStepReply uses record spread: set((state) => ({ stepReplies: { ...state.stepReplies, [stepId]: reply } }))

    appendReplyFeedEntry prepends the entry and slices to 500 (FIFO-500, D-11): set((s) => ({ planReplyFeed: [entry, ...s.planReplyFeed].slice(0, 500) }))

    setPaneMode is a scalar set: set({ paneMode: mode })
  </action>
  <verify>
    <automated>cd /Users/majesnix/gits/proto-sender && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/stores/usePlanExecutionStore.ts imports ReplyMessage and FeedMessage from "../lib/types"
    - PlanExecutionState interface contains stepReplies: Record<string, ReplyMessage>, planReplyFeed: FeedMessage[], paneMode: 'editor' | 'reply'
    - INITIAL_STATE contains stepReplies, planReplyFeed, and paneMode fields
    - setRunning action body contains literal stepReplies: {}, planReplyFeed: [], paneMode: 'editor' (grep: `grep -n "planReplyFeed: \[\]" src/stores/usePlanExecutionStore.ts` finds at least 2 occurrences — one in INITIAL_STATE, one in setRunning)
    - appendReplyFeedEntry implementation contains .slice(0, 500)
    - setStepReply implementation uses spread: { ...state.stepReplies, [stepId]: reply }
    - tsc exits 0 with no new errors
  </acceptance_criteria>
  <done>Store has all three new fields, four new actions, INITIAL_STATE is updated, setRunning resets them inline, tsc exits 0</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire usePlanRunner to dispatch store actions after reply-bearing steps</name>
  <files>src/hooks/usePlanRunner.ts</files>
  <read_first>
    src/hooks/usePlanRunner.ts — read the full file to see the runner loop (lines 54-98), current destructure block (lines 19-29), and the if (result.status === 'done') branch before editing
    src/stores/usePlanExecutionStore.ts — confirm the three new action signatures just added in Task 1
    src/lib/types.ts — confirm StepResult.reply shape and FeedMessage fields
  </read_first>
  <behavior>
    - For a step returning result.status === 'done' with result.reply !== null: setStepReply is called, setPaneMode('reply') is called, appendReplyFeedEntry is called — all three in that order
    - For a step returning result.status === 'done' with result.reply === null (no-wait step): none of the three actions are called
    - For a step returning result.status === 'error': none of the three actions are called
    - setPaneMode('editor') is called before each executeStep await — before the reply-mode transition can happen — so the pane is always reset to editor when the next step starts
    - The FeedMessage built inline for appendReplyFeedEntry uses crypto.randomUUID() for id, '' for exchange, Date.now() / 1000 for timestamp, null for error
    - succeeded++ is incremented after the reply dispatch block (not inside it)
  </behavior>
  <action>
    Destructure the three new actions from usePlanExecutionStore() alongside the existing destructure:
      setStepReply, setPaneMode, appendReplyFeedEntry

    In the runner loop, before the const result = await executeStep(activeProfileName, step) call (currently around line 67), add:
      setPaneMode('editor')
    This implements D-04's "switch back to form for next step" behavior.

    Extend the if (result.status === 'done') branch. After the existing setStepStatus(step.id, 'done') call, insert the reply dispatch block (before succeeded++):
      if (result.reply !== null) {
        setStepReply(step.id, result.reply)
        setPaneMode('reply')
        appendReplyFeedEntry({
          id: crypto.randomUUID(),
          routingKey: result.reply.routingKey,
          exchange: '',
          contentType: result.reply.contentType,
          timestamp: Date.now() / 1000,
          decoded: result.reply.decoded,
          hexString: result.reply.hexString,
          error: null,
          decodedAs: result.reply.decodedAs,
        })
      }

    Do not modify the error branch or the catch block. No-wait steps return result.reply === null — the if-guard ensures they contribute nothing to the feed (D-10 scope).
  </action>
  <verify>
    <automated>cd /Users/majesnix/gits/proto-sender && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - usePlanRunner.ts destructures setStepReply, setPaneMode, appendReplyFeedEntry from usePlanExecutionStore()
    - setPaneMode('editor') appears before the executeStep call (grep: `grep -n "setPaneMode" src/hooks/usePlanRunner.ts` shows 'editor' call on a lower line number than the executeStep call)
    - The reply dispatch block is inside `if (result.status === 'done')` nested inside `if (result.reply !== null)` — grep: `grep -n "result.reply !== null" src/hooks/usePlanRunner.ts` returns a match
    - appendReplyFeedEntry inline object contains exchange: '' and error: null
    - appendReplyFeedEntry call is NOT present outside of the `result.reply !== null` guard (no-wait safety)
    - tsc exits 0 with no new errors
  </acceptance_criteria>
  <done>Runner dispatches setStepReply, setPaneMode, and appendReplyFeedEntry only for reply-bearing steps; paneMode resets to 'editor' at the start of each step; tsc exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri IPC → Frontend | StepResult.reply data arrives from Rust backend via type-safe IPC; already validated in Phase 22 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Information Disclosure | usePlanExecutionStore (in-memory) | accept | Ephemeral store; no persist middleware; data cleared on app restart; same risk profile as stepStatuses already in store |
| T-23-02 | Tampering | appendReplyFeedEntry FIFO slice | mitigate | Immutable spread + .slice(0, 500) prevents unbounded growth; no in-place mutation per coding-style.md CRITICAL rule |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase; all deps audited in prior phases (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0 with no new type errors
2. `grep -n "stepReplies: {}" src/stores/usePlanExecutionStore.ts` returns at least 2 matches (INITIAL_STATE + setRunning)
3. `grep -n "planReplyFeed: \[\]" src/stores/usePlanExecutionStore.ts` returns at least 2 matches
4. `grep -n "paneMode: 'editor'" src/stores/usePlanExecutionStore.ts` returns at least 2 matches
5. `grep -n "slice(0, 500)" src/stores/usePlanExecutionStore.ts` returns 1 match
6. `grep -n "result.reply !== null" src/hooks/usePlanRunner.ts` returns 1 match inside the done branch
7. `grep -n "setPaneMode" src/hooks/usePlanRunner.ts` returns 2 matches ('editor' before executeStep, 'reply' inside reply block)
</verification>

<success_criteria>
- usePlanExecutionStore exported with stepReplies, planReplyFeed, paneMode fields and setStepReply, appendReplyFeedEntry, setPaneMode, clearRun actions
- setRunning resets all three new fields inline (not via clearRun)
- usePlanRunner calls setPaneMode('editor') before executeStep and dispatches the three reply actions only when result.reply is non-null
- tsc exits 0
</success_criteria>

<output>
Create `.planning/phases/23-response-view-inline-and-shared-feed/23-01-SUMMARY.md` when done
</output>
