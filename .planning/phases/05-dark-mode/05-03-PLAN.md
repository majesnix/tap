---
phase: 05-dark-mode
plan: 03
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified: []
autonomous: false
requirements:
  - DRK-04

must_haves:
  truths:
    - "Every UI surface renders without visual defects in dark mode"
    - "No invisible text (foreground blends with background)"
    - "No missing borders or separators"
    - "No washed-out icons or action buttons"
    - "Sonner toasts display correctly in both modes"
    - "All shadcn/ui component types (inputs, selects, switches, dialogs, sheets) render correctly"
  artifacts:
    - path: ".planning/phases/05-dark-mode/05-HUMAN-UAT.md"
      provides: "Completed manual UAT checklist"
      contains: "DRK-04"
  key_links:
    - from: "app running in dark mode"
      to: "every UI surface in UI-SPEC DRK-04 matrix"
      via: "manual visual inspection by human"
      pattern: "walkthrough"
---

<objective>
Human visual UAT walkthrough of every existing UI surface in dark mode. This checkpoint verifies DRK-04: all existing UI surfaces render correctly in dark mode after Plans 01 and 02 are shipped.

Purpose: Per D-07, DRK-04 is verified by manual visual UAT only. The CSS variables are already complete — what matters is visual inspection of the actual rendered output.

Output: Human sign-off on the DRK-04 surface checklist below.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dark-mode/05-UI-SPEC.md
@.planning/phases/05-dark-mode/05-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: DRK-04 Manual Visual UAT — Dark Mode Surface Walkthrough</name>
  <what-built>
    Plans 05-01 and 05-02 have been executed:
    - App.tsx now wraps everything in ThemeProvider (DRK-01: OS preference auto-detected, DRK-03: mode persists via tauri-plugin-store)
    - Sidebar footer has the ThemeToggle icon cycle button (DRK-02: user can switch modes)

    The dark mode CSS variables were already complete in src/index.css (oklch .dark {} block at lines 152-184). This UAT verifies that the completed CSS + next-themes integration produce correct visuals across all surfaces.
  </what-built>
  <how-to-verify>
    Prerequisites:
    1. Run the app: `npm run tauri dev` (from the project root)
    2. Switch to dark mode using the ThemeToggle button in the sidebar footer (Monitor → Sun → Moon — click twice to reach Moon/dark)

    Verify each surface below. For each surface, confirm:
    - Text is legible (no invisible text — foreground not blending with background)
    - Borders and separators are visible
    - Icons and buttons have sufficient contrast
    - Interactive elements (hover, focus) show visible state changes

    SURFACE CHECKLIST — check each in dark mode:

    Left Sidebar:
    - [ ] Sidebar structure — background, separators, section headers ("Proto Sender" heading, "Load a .proto file" caption)
    - [ ] File section (FileSection.tsx) — "Load .proto file" button, include path list items
    - [ ] Connection section (ConnectionSection.tsx) — profile dropdown, status dot (green/red/yellow), "Manage..." button
    - [ ] Message type select — "Select message..." dropdown trigger + content list
    - [ ] Footer — version string "v0.1.0 — Walking Skeleton" legible; ThemeToggle button visible with icon; hover state visible

    Form Panel:
    - [ ] Form panel container (FormPanel.tsx) — background, encode status badge (green/red)
    - [ ] Scalar fields (all input types: text, number, bool checkbox) — input borders, labels, validation error text
    - [ ] Nested message fields (NestedMessageField.tsx) — collapse/expand chevron, nested section border
    - [ ] Repeated fields (RepeatedField.tsx) — add/remove row controls, row separators
    - [ ] Enum fields (EnumField.tsx) — Select dropdown trigger and content
    - [ ] Oneof fields (OneofField.tsx) — RadioGroup items, branch selector
    - [ ] Well-known type fields (WellKnownTypeField.tsx) — Duration/Timestamp dual inputs

    Publish Bar:
    - [ ] Publish bar (PublishBar.tsx) — exchange/queue picker, routing key input, "Publish" button
    - [ ] AMQP Properties Sheet (AmqpPropertiesSheet.tsx) — open the sheet; verify all fields, content-type, delivery mode switch, TTL, headers table; Apply/Cancel buttons

    Right Panel:
    - [ ] Right panel tabs (RightPanel.tsx) — tab triggers (Form / History / Response), active tab indicator
    - [ ] History panel (HistoryPanel.tsx) — entry list rows, filter controls, replay/resend button, status badges (sent/failed)
    - [ ] Response tab — queue picker (Live/Manual toggle, queue input field), decoded view (collapsible tree), hex section (copy buttons), empty state text

    Modals and Overlays:
    - [ ] Connection profile modal (Dialog) — "Add Profile" / edit modal; all inputs (name, host, port, vhost), test button, save button
    - [ ] Any AlertDialog confirm/cancel dialogs — open one (e.g., delete profile) and verify button contrast
    - [ ] Sonner toasts — trigger a success toast (send a message) and an error toast (connect with wrong credentials); verify both are legible

    shadcn/ui Component Sweep:
    - [ ] Input fields — focused state ring visible
    - [ ] Select (Radix) — open state, item highlight on hover
    - [ ] Switch — checked/unchecked states clearly distinct
    - [ ] RadioGroup — selected/unselected item visual difference
    - [ ] Tabs — active/inactive tab clear distinction
    - [ ] Popover/Tooltip — content background not transparent
    - [ ] Badge — status colors readable
    - [ ] ScrollArea — scrollbar visible or at minimum functional
    - [ ] Separator — visible (not blending with background)

    Theme Toggle Behavior:
    - [ ] Cycle through all three modes: Moon (dark) → Monitor (system) → Sun (light) → Moon (dark)
    - [ ] Each transition applies immediately (no reload)
    - [ ] In system mode: matches OS preference (if possible to verify — e.g., change OS dark/light setting while app is running)
    - [ ] Restart the app: previously selected mode is restored (DRK-03 cross-check)

    If any surface has a visual defect, describe it in the resume signal below.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all surfaces pass visual inspection.
    Or describe any defects found (e.g., "History panel entry text invisible in dark mode — foreground same as background").
    If defects are found, a gap plan (05-04-PLAN.md) will be created to fix them.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human → App | Human tester interacts with the running app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Information Disclosure | UAT checklist results | accept | UAT results contain no sensitive data — only visual pass/fail observations about UI rendering. Results stored in planning artifact only, not in application code. |
</threat_model>

<verification>
This plan's verification IS the human UAT walkthrough. No automated verification applies to DRK-04 per D-07.

After the UAT checkpoint resolves:
- If approved: create .planning/phases/05-dark-mode/05-HUMAN-UAT.md recording the sign-off date and any observations
- If defects found: create gap plan 05-04-PLAN.md targeting specific surfaces with visual defects
</verification>

<success_criteria>
- Human has walked through every surface in the DRK-04 checklist (all 30+ items)
- Human has confirmed all surfaces render without visual defects in dark mode
- ThemeToggle cycle behavior verified across all three modes
- Theme persistence verified across an app restart
- .planning/phases/05-dark-mode/05-HUMAN-UAT.md created recording sign-off
</success_criteria>

<output>
After completion, create `.planning/phases/05-dark-mode/05-03-SUMMARY.md`
</output>
