---
phase: 18-auto-update-linux-docs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release.yml
  - docs/linux-keychain.md
autonomous: true
requirements:
  - PKG-01
  - UPD-04
  - DOC-01

must_haves:
  truths:
    - "release.yml build-linux tauri-action step has TAURI_SIGNING_PRIVATE_KEY and TAURI_SIGNING_PRIVATE_KEY_PASSWORD in its env block"
    - "release.yml build-windows job is deleted entirely"
    - "docs/linux-keychain.md exists with libsecret runtime prerequisite documentation"
    - "docs/linux-keychain.md documents distro-specific install commands for Ubuntu/Debian, Fedora, and Arch"
    - "docs/linux-keychain.md clarifies that dbus-secret-service-keyring-store does NOT require libsecret-1-dev"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Linux signing env vars + Windows job removal"
      contains: "TAURI_SIGNING_PRIVATE_KEY"
    - path: "docs/linux-keychain.md"
      provides: "libsecret runtime prerequisite documentation for Linux users"
      min_lines: 40
      contains: "gnome-keyring"
  key_links:
    - from: ".github/workflows/release.yml build-linux job"
      to: "TAURI_SIGNING_PRIVATE_KEY GitHub secret"
      via: "${{ secrets.TAURI_SIGNING_PRIVATE_KEY }} env var on tauri-action step"
      pattern: "TAURI_SIGNING_PRIVATE_KEY"
---

<objective>
Wire the Linux CI signing env vars, remove the out-of-scope Windows job, and create the Linux keychain documentation.

Purpose: PKG-01 — Linux AppImage must be Ed25519-signed by the CI job so latest.json gets a linux-x86_64 entry with a non-empty signature. Without signing env vars on the Linux job, tauri-action silently skips .sig upload. DOC-01 — Linux users need documentation on the libsecret runtime prerequisite.

Output: release.yml with Linux signing env vars + Windows job deleted; docs/linux-keychain.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-auto-update-linux-docs/18-RESEARCH.md
@.planning/phases/18-auto-update-linux-docs/18-PATTERNS.md
</context>

<interfaces>
<!-- Current state of relevant release.yml sections. Extracted from codebase. -->

From .github/workflows/release.yml:

Linux job build step (lines 127-135) — CURRENT (missing signing):
```yaml
- name: Build Tauri app (Linux x86_64)
  uses: tauri-apps/tauri-action@v0
  env:
    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  with:
    tagName: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || '' }}
    releaseName: "Tap v__VERSION__"
    releaseDraft: true
    releasePrerelease: ${{ contains(github.ref_name, '-') }}
```

Windows job (lines 137-173) — DELETE ENTIRELY:
```yaml
build-windows:
  runs-on: windows-latest
  ...
```

macOS env block (lines 66-79) — reference pattern only (do not modify):
```yaml
env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
  ...
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Linux signing env vars and delete Windows job from release.yml</name>
  <files>.github/workflows/release.yml</files>
  <read_first>
    - .github/workflows/release.yml (full file — current state; lines 127-135 are the Linux tauri-action step; lines 137-173 are the build-windows job to delete)
    - .planning/phases/18-auto-update-linux-docs/18-RESEARCH.md (Pattern 6: CI Signing Env Vars, Pitfall 2: Linux Job Missing Signing Env Vars, Pitfall 5: Windows Job Still in release.yml)
    - .planning/phases/18-auto-update-linux-docs/18-PATTERNS.md (Pattern Assignment: .github/workflows/release.yml — build-linux job; build-windows job deletion)
  </read_first>
  <action>
**Change 1 — Add signing env vars to build-linux tauri-action step:**

Replace the Linux tauri-action step (lines 127-135) with:
```yaml
      - name: Build Tauri app (Linux x86_64)
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tagName: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || '' }}
          releaseName: "Tap v__VERSION__"
          releaseDraft: true
          releasePrerelease: ${{ contains(github.ref_name, '-') }}
```

Keep `releaseDraft: true` — consistent with the macOS job and the Phase 17 review workflow. The update endpoint requires a published release; publishing the draft is documented as a manual release step in docs/release-setup.md.

`TAURI_SIGNING_PRIVATE_KEY_PASSWORD` is kept present for safety — if the key was generated without a passphrase, the GitHub secret can be added with an empty value and tauri-action handles the empty-password case.

Do NOT modify the macOS job (lines 10-86) or any other existing step in build-linux.

**Change 2 — Delete the build-windows job entirely:**

Delete lines 137-173 (the entire `build-windows:` job block). Windows distribution is explicitly out of scope in v1.5 per REQUIREMENTS.md. Deleting now prevents CI minutes waste and removes a potential release blocker if Windows signing is missing.

After deletion, the YAML file must end after the last line of the `build-linux` job. Verify there is no dangling YAML at the end of the file.
  </action>
  <verify>
    <automated>grep -c 'TAURI_SIGNING_PRIVATE_KEY' .github/workflows/release.yml && grep -c 'build-windows' .github/workflows/release.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'TAURI_SIGNING_PRIVATE_KEY' .github/workflows/release.yml | grep -v PASSWORD` returns exactly one line: `          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}`
    - `grep 'TAURI_SIGNING_PRIVATE_KEY_PASSWORD' .github/workflows/release.yml` returns exactly one line in the Linux env block
    - `grep 'build-windows' .github/workflows/release.yml` returns nothing (job fully deleted)
    - `grep 'windows-latest' .github/workflows/release.yml` returns nothing (runner reference also gone)
    - `grep 'releaseDraft: true' .github/workflows/release.yml` returns exactly 2 lines (macOS job and Linux job — NOT 3 as before)
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"` exits 0 (valid YAML)
  </acceptance_criteria>
  <done>release.yml has TAURI_SIGNING_PRIVATE_KEY + PASSWORD in Linux env block, build-windows job deleted, YAML is valid.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create docs/linux-keychain.md</name>
  <files>docs/linux-keychain.md</files>
  <read_first>
    - docs/release-setup.md (structural analog — H1 title, one-sentence purpose, --- separators, numbered steps, code blocks, bold for field names, verification section at bottom)
    - .planning/phases/18-auto-update-linux-docs/18-RESEARCH.md (DOC-01 Content Outline section — 6 required topics with exact content notes)
    - .planning/phases/18-auto-update-linux-docs/18-PATTERNS.md (Pattern Assignment: docs/linux-keychain.md — content sections, important content note)
  </read_first>
  <action>
Create docs/linux-keychain.md with the following content. Match the tone and structure of docs/release-setup.md (terse, developer-facing, imperative steps).

```markdown
# Linux Keychain Setup

Tap stores RabbitMQ connection credentials (AMQP URIs including passwords) in the OS
keychain using the D-Bus Secret Service API. On Linux this requires a running Secret
Service daemon such as GNOME Keyring or KWallet.

---

## What Tap stores in the keychain

Tap stores AMQP connection URIs containing passwords. It uses the
`dbus-secret-service-keyring-store` crate with the `crypto-rust` feature, which
communicates with the Secret Service protocol over D-Bus directly. It does **not** use the
`libsecret` C API or require `libsecret-1-dev` at runtime.

---

## Runtime dependencies

You need one of:

- **`gnome-keyring`** — GNOME/GTK environments (most Ubuntu and Fedora desktops)
- **`kwallet`** — KDE Plasma environments
- Any other D-Bus Secret Service-compatible daemon (e.g., `secret-service` from the
  Secret Service specification)

The underlying D-Bus client library (`libdbus-1-3`) is typically pre-installed on all
major Linux desktop distributions.

---

## Step 1: Install GNOME Keyring

**Ubuntu / Debian:**
```bash
sudo apt-get install gnome-keyring
```

**Fedora / RHEL:**
```bash
sudo dnf install gnome-keyring
```

**Arch Linux:**
```bash
sudo pacman -S gnome-keyring
```

---

## Step 2: Ensure the daemon is running

On a standard desktop session, GNOME Keyring starts automatically at login. If it is not
running (e.g., after first install or in a minimal session), start it manually:

```bash
gnome-keyring-daemon --start --components=secrets
```

---

## Headless / server environments

If no Secret Service daemon is available, Tap will fail to save or load connection
profiles with a keychain error at startup. On headless systems you must either:

1. Run `gnome-keyring-daemon --start --components=secrets` before launching Tap, or
2. Use a virtual display and session manager that starts a keyring daemon automatically.

Tap is a desktop dev tool and is not designed for headless server use.

---

## Verification

Check that a Secret Service daemon is accessible via D-Bus:

```bash
# Should print the Secret Service introspection XML (no error = daemon is running)
gdbus introspect --session --dest org.freedesktop.secrets \
  --object-path /org/freedesktop/secrets
```

Alternatively:
```bash
# List secrets (requires gnome-keyring running; output may be empty on a fresh install)
secret-tool search --all application tap 2>/dev/null || echo "secret-tool not found — install libsecret-tools"
```
```

Ensure the file ends with a single newline. Do not add trailing blank lines.
  </action>
  <verify>
    <automated>test -f docs/linux-keychain.md && wc -l docs/linux-keychain.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/linux-keychain.md` exits 0
    - `grep 'gnome-keyring' docs/linux-keychain.md` returns at least 3 lines (install section + daemon section + heading)
    - `grep 'apt-get install gnome-keyring' docs/linux-keychain.md` confirms Ubuntu/Debian install command
    - `grep 'dnf install gnome-keyring' docs/linux-keychain.md` confirms Fedora install command
    - `grep 'pacman -S gnome-keyring' docs/linux-keychain.md` confirms Arch install command
    - `grep 'libsecret' docs/linux-keychain.md` returns the line clarifying it is NOT required (contains "does not" or "not require" near "libsecret")
    - `grep 'Headless' docs/linux-keychain.md` confirms headless warning section exists
    - `wc -l docs/linux-keychain.md` returns at least 40 lines
  </acceptance_criteria>
  <done>docs/linux-keychain.md exists with all 6 required content topics from RESEARCH.md DOC-01 outline: what is stored, runtime deps, system libraries, headless warning, verification command, distro-specific install commands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions secrets → CI env | TAURI_SIGNING_PRIVATE_KEY is a CI secret; never logged, never in release.yml plaintext |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-09 | Information Disclosure | TAURI_SIGNING_PRIVATE_KEY in release.yml | mitigate | Secret referenced via ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }} — GitHub Actions masks secrets in logs automatically |
| T-18-10 | Tampering | Linux AppImage missing .sig | mitigate | Adding TAURI_SIGNING_PRIVATE_KEY env var to Linux job ensures tauri-action produces .AppImage.sig; Plan 04 verification confirms sig present |
| T-18-11 | Information Disclosure | docs/linux-keychain.md | accept | Documentation file has no secrets; explains public runtime requirements only |
</threat_model>

<verification>
After all tasks complete:

1. `grep 'TAURI_SIGNING_PRIVATE_KEY' .github/workflows/release.yml` — returns lines in Linux env block only
2. `grep 'build-windows' .github/workflows/release.yml` — returns nothing
3. `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"` — exits 0
4. `test -f docs/linux-keychain.md && echo exists` — prints "exists"
5. `grep 'gnome-keyring' docs/linux-keychain.md | wc -l` — returns 3 or more
</verification>

<success_criteria>
- release.yml Linux job has TAURI_SIGNING_PRIVATE_KEY + TAURI_SIGNING_PRIVATE_KEY_PASSWORD in env block
- release.yml build-windows job is completely removed
- YAML is valid (python3 yaml.safe_load exits 0)
- docs/linux-keychain.md exists with all 6 content topics from DOC-01 outline
</success_criteria>

<output>
After completion, create `.planning/phases/18-auto-update-linux-docs/18-03-SUMMARY.md`
</output>
