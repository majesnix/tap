---
phase: 26-block-apply-conflict-prompt-oneof
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - src/components/form/FormPanel.tsx
  - src/components/form/ProtoFormRenderer.tsx
autonomous: true
requirements:
  - BLK-EXT-03
  - BLK-EXT-04
  - BLK-EXT-05
  - BLK-EXT-06

must_haves:
  truths:
    - "BLK-EXT-06: BlockApplyConflictDialog opens (AlertDialog open=true) when plan.conflicts.length > 0 after buildPlan in onDragEnd; skipped entirely when conflicts is empty (existing Phase 25 commitApply flow unchanged)"
    - "BLK-EXT-06: D-04: branch-switch conflict rows appear in the same batched dialog as map-collision and dirty-subfield rows"
    - "Pitfall E: all conflict rows default to skip — conflictChoices state initialized to {} (empty); RadioGroup defaultValue='skip'; ConflictChoices read with ?? 'skip'"
    - "Apply block calls commitApply(conflictPlan, conflictChoices) and closes the dialog; form fields for skip choices are NOT written"
    - "Discard block / Escape closes the dialog without calling commitApply; no form state is changed"
    - "D-05: branch-switch overwrite → commitApply performs single atomic setValue(fieldName, { _selected: newBranch, [newBranch]: subValue }, { shouldDirty: false })"
    - "D-06: branch-switch skip → no partial write; that oneof field is fully excluded from commit"
    - "Pitfall D: all setValue calls in commitApply (new and existing) use { shouldDirty: false }"
    - "BLK-EXT-03: map_key_collision overwrite → existing rows kept, colliding rows replaced with block value, non-colliding block rows appended; uses mapReplaceRegistry"
    - "BlockApplyConflictDialog is inline in FormPanel.tsx — NOT a separate file (BLK-EXT-07 locked)"
    - "Row label formats match UI-SPEC exactly: map key collision uses collisionKey, branch-switch uses currentBranch/blockBranch, dirty-subfield uses subFieldName"
    - "AlertDialogContent has className='sm:max-w-lg'; AlertDialogCancel receives autoFocus"
    - "Conflict row count N in description uses correct singular/plural: '1 field already has a value.' vs 'N fields already have values.'"
  artifacts:
    - path: "src/components/form/FormPanel.tsx"
      provides: "BlockApplyConflictDialog component inline, conflictPlan/conflictChoices state, onDragEnd gate"
      contains: "BlockApplyConflictDialog"
    - path: "src/components/form/ProtoFormRenderer.tsx"
      provides: "Extended commitApply accepting choices param, atomic oneof write, map overwrite via registry, shouldDirty:false on all setValue"
      contains: "shouldDirty: false"
  key_links:
    - from: "FormPanel.tsx — onDragEnd"
      to: "BlockApplyConflictDialog"
      via: "setConflictPlan(plan); setConflictChoices({})"
      pattern: "conflictPlan"
    - from: "BlockApplyConflictDialog — Apply button"
      to: "applyBlockRef.current.commitApply"
      via: "commitApply(conflictPlan, conflictChoices)"
      pattern: "commitApply\\(conflictPlan"
    - from: "ProtoFormRenderer.tsx — commitApply"
      to: "mapReplaceRegistry"
      via: "merged rows array passed to mapReplaceRegistry.current[item.fieldName](mergedRows)"
      pattern: "mapReplaceRegistry\\.current"
    - from: "commitApply — oneof branch-switch"
      to: "methods.setValue (atomic)"
      via: "setValue(item.fieldName, { _selected: blockBranch, [blockBranch]: subValue }, { shouldDirty: false })"
      pattern: "shouldDirty.*false"
---

<objective>
Wire the conflict dialog UX and extend commitApply to execute user conflict choices. This plan delivers the full user-facing conflict resolution flow: drag block → dialog appears for conflicts → user chooses skip/overwrite per row → Apply or Discard.

Purpose: Plans 26-01 extended the pure detection logic. This plan connects it to the UI and write layer. After this plan, all four BLK-EXT-03/04/05/06 requirements are met end-to-end.

Output:
- `FormPanel.tsx`: inline `BlockApplyConflictDialog` component, `conflictPlan`/`conflictChoices` state, updated `onDragEnd` gate
- `ProtoFormRenderer.tsx`: `commitApply` accepts `choices?: ConflictChoices`, implements three overwrite branches (map merge, oneof dirty-subfield dotted-path, oneof branch-switch atomic), `shouldDirty: false` on ALL setValue calls
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-block-apply-conflict-prompt-oneof/26-CONTEXT.md
@.planning/phases/26-block-apply-conflict-prompt-oneof/26-RESEARCH.md
@.planning/phases/26-block-apply-conflict-prompt-oneof/26-UI-SPEC.md
@.planning/phases/26-block-apply-conflict-prompt-oneof/26-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 26-01 output — executor reads these from blockApply.ts after 26-01 completes -->

From src/lib/blockApply.ts (post-26-01):
```typescript
export type ApplyItemKind = "scalar" | "enum" | "well_known" | "map" | "oneof";

export type ConflictItemKind =
  | 'map_key_collision'
  | 'oneof_dirty_subfield'
  | 'oneof_branch_switch';

export type ConflictItem = {
  fieldName: string;
  blockValue: unknown;
  currentValue: unknown;
  kind: ConflictItemKind;
  subFieldName?: string;      // oneof_dirty_subfield only
  currentBranch?: string;     // oneof_branch_switch only
  blockBranch?: string;       // oneof_branch_switch only
  collisionKey?: string;      // map_key_collision only
};

export type ConflictChoices = Record<string, 'skip' | 'overwrite'>;
// Compound key format:
//   map_key_collision:      "{fieldName}:{collisionKey}"
//   oneof_dirty_subfield:   "{fieldName}:{subFieldName}"
//   oneof_branch_switch:    "{fieldName}"  (bare field name)

export type ApplyBlockRef = {
  buildPlan: (blockValues: Record<string, unknown>) => ApplyPlan;
  commitApply: (plan: ApplyPlan, choices?: ConflictChoices) => void;
};

export type ApplyPlan = {
  toApply: ApplyItem[];
  conflicts: ConflictItem[];
  unknownKeys: string[];
};
```

From src/components/form/FormPanel.tsx (current — lines 55-83, pre-Phase-26):
```typescript
// onDragEnd currently (line 75-77):
const plan = applyBlockRef.current.buildPlan(blockValues);
// plan.conflicts is always [] in Phase 25 — Phase 26 adds the conflict dialog
applyBlockRef.current.commitApply(plan);
```

From src/components/form/ProtoFormRenderer.tsx (current — lines 172-188, pre-Phase-26):
```typescript
commitApply: (plan) => {
  for (const item of plan.toApply) {
    if (item.kind === "map") {
      mapReplaceRegistry.current[item.fieldName]?.(item.value as unknown[]);
    } else {
      // Pitfall D regression: currently omits { shouldDirty: false }
      methods.setValue(
        item.fieldName,
        item.value as Parameters<typeof methods.setValue>[1]
      );
    }
  }
},
```

From src/components/ui/ (confirmed present — no imports to add):
- alert-dialog: AlertDialog, AlertDialogContent, AlertDialogHeader, AlertDialogTitle,
    AlertDialogDescription, AlertDialogFooter, AlertDialogCancel, AlertDialogAction
- radio-group: RadioGroup, RadioGroupItem
- badge: Badge
- scroll-area: ScrollArea
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add BlockApplyConflictDialog + conflict state + onDragEnd gate to FormPanel.tsx</name>
  <files>src/components/form/FormPanel.tsx</files>

  <read_first>
    - src/components/form/FormPanel.tsx — full file (read entire file; understand import list, state declarations, useDndMonitor onDragEnd at lines 55-83, return JSX at lines 230-292)
    - src/lib/blockApply.ts — verify ConflictChoices, ConflictItem, ConflictItemKind exports exist (post 26-01)
    - src/components/ui/alert-dialog.tsx — verify exported names (AlertDialogContent, etc.)
    - src/components/ui/radio-group.tsx — verify RadioGroup, RadioGroupItem export names
    - src/components/ui/badge.tsx — verify Badge export name
  </read_first>

  <behavior>
    - After Apply block click: applyBlockRef.current?.commitApply(conflictPlan, conflictChoices) is called
    - After Discard block click: commitApply is NOT called; form values unchanged
    - When plan.conflicts.length > 0: setConflictPlan(plan) + setConflictChoices({}) is called instead of immediate commitApply
    - When plan.conflicts.length === 0: immediate commitApply(plan) (no dialog, existing Phase 25 behavior)
    - Row radio change: setConflictChoices(prev => ({ ...prev, [choiceKey]: value })) — immutable update
    - Dialog only renders when conflictPlan !== null (conditional, not hidden)
  </behavior>

  <action>
    Read FormPanel.tsx fully before editing. Then make the following targeted changes:

    1. Add imports at the top of the import block (after existing @/lib/blockApply import):
       - From "@/lib/blockApply": also import ConflictChoices, ConflictItem, ConflictItemKind (add to the existing `import type { ApplyBlockRef }` line or add a second import type line)
       - From "@/components/ui/alert-dialog": AlertDialog, AlertDialogContent, AlertDialogHeader, AlertDialogTitle, AlertDialogDescription, AlertDialogFooter, AlertDialogCancel, AlertDialogAction
       - From "@/components/ui/radio-group": RadioGroup, RadioGroupItem
       - From "@/components/ui/badge": Badge
       - ApplyPlan type is needed for conflictPlan state — add to blockApply import

    2. Add two state declarations after the existing `const applyBlockRef = useRef(...)` line (around line 51):
       - `const [conflictPlan, setConflictPlan] = useState<ApplyPlan | null>(null);`
       - `const [conflictChoices, setConflictChoices] = useState<ConflictChoices>({});`

    3. Replace the onDragEnd block at lines 75-77 (the immediate commitApply call) with the conflict gate:
       ```
       const plan = applyBlockRef.current.buildPlan(blockValues);
       if (plan.conflicts.length > 0) {
         setConflictPlan(plan);
         setConflictChoices({});  // Pitfall E: all rows default to skip via ?? 'skip' in dialog
       } else {
         applyBlockRef.current.commitApply(plan);
       }
       ```
       Keep the unknownKeys toast that follows (lines 78-82) — it runs unconditionally after buildPlan regardless of conflict path.

    4. Define BlockApplyConflictDialog as an inline function component INSIDE FormPanel (before the return statement, after the handleDiscard function). The component is local to FormPanel.tsx (BLK-EXT-07 locked). It must NOT be extracted to a separate file.

       BlockApplyConflictDialog receives no props — it closes over `conflictPlan`, `conflictChoices`, `setConflictChoices`, `applyBlockRef`, `setConflictPlan` from the FormPanel closure.

       Structure (follow 26-UI-SPEC.md exactly):
       - AlertDialog open={conflictPlan !== null} onOpenChange is not needed (cancel/apply handle close explicitly)
       - AlertDialogContent className="sm:max-w-lg"
       - AlertDialogHeader: AlertDialogTitle "Review conflicts", AlertDialogDescription showing N with singular/plural: if N === 1: "1 field already has a value. Choose what to do." else: "{N} fields already have values. Choose what to do for each."
       - Scroll container: div with className="max-h-[50vh] overflow-y-auto px-6" wrapping the conflict rows
       - For each conflict item (conflictPlan?.conflicts ?? []):
         - Compute the ConflictChoices key for this item:
           - map_key_collision: `${item.fieldName}:${item.collisionKey}`
           - oneof_dirty_subfield: `${item.fieldName}:${item.subFieldName}`
           - oneof_branch_switch: `${item.fieldName}`
         - Compute the row label (from UI-SPEC row label formats table):
           - map_key_collision: `"${fieldLabel}" — key "${item.collisionKey}" already exists`  (fieldLabel = item.fieldName — executor finds the matching FieldSchema label from conflictPlan? No. fieldLabel = item.fieldName directly since ConflictItem has fieldName but not fieldLabel — use item.fieldName as the label)
           - oneof_dirty_subfield: `"${item.fieldName}.${item.subFieldName}" already has a value`
           - oneof_branch_switch: `Switch "${item.fieldName}" from "${item.currentBranch}" to "${item.blockBranch}"`
         - Compute current choice: `conflictChoices[choiceKey] ?? 'skip'`
         - Compute badge text: map_key_collision → "map key", oneof_dirty_subfield → "dirty field", oneof_branch_switch → "branch switch"
         - Render: div with className="flex items-start gap-2 border-b border-border py-2 last:border-0"
           - RadioGroup value={currentChoice} onValueChange={(v) => setConflictChoices(prev => ({ ...prev, [choiceKey]: v as 'skip' | 'overwrite' }))} className="flex flex-col gap-1"
             - RadioGroupItem value="skip" id={`skip-${choiceKey}`} with label "Skip"
             - RadioGroupItem value="overwrite" id={`overwrite-${choiceKey}`} with label "Overwrite"
           - div className="flex-1 flex flex-col gap-1"
             - span className="text-xs font-semibold": row label text
             - Badge variant="outline" className="text-xs w-fit": badge text
             - span className="text-xs text-muted-foreground": current value preview (JSON.stringify truncated to 60 chars with "..." if longer)
       - AlertDialogFooter:
         - AlertDialogCancel autoFocus onClick={() => setConflictPlan(null)}: "Discard block"
         - AlertDialogAction onClick={() => { if (conflictPlan && applyBlockRef.current) { applyBlockRef.current.commitApply(conflictPlan, conflictChoices); } setConflictPlan(null); }}: "Apply block"

    5. In the return JSX, add `<BlockApplyConflictDialog />` ABOVE the outer div (or just before the closing tag) — it is always mounted but the AlertDialog open prop controls visibility. Since it renders nothing when conflictPlan is null, placement before the return div is fine. Do NOT nest it inside the ScrollArea.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "FormPanel|blockApply" | head -20</automated>
    No TypeScript errors on FormPanel.tsx or blockApply imports.
  </verify>

  <done>
    FormPanel.tsx has: conflict state declarations, updated onDragEnd gate, inline BlockApplyConflictDialog following UI-SPEC. TypeScript compiles without errors. No runtime test possible at this stage (commitApply signature update in Task 2 required).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend commitApply in ProtoFormRenderer.tsx with choices param + three overwrite branches + shouldDirty:false</name>
  <files>src/components/form/ProtoFormRenderer.tsx</files>

  <read_first>
    - src/components/form/ProtoFormRenderer.tsx — read lines 1-50 (imports, type declarations) and lines 155-195 (applyBlockRef useEffect containing buildPlan + commitApply). Check mapReplaceRegistry usage at lines 120-135.
    - src/lib/blockApply.ts — verify ConflictChoices, ConflictItem, ConflictItemKind are exported; read ApplyItem type to understand toApply item shape for 'oneof' kind
  </read_first>

  <behavior>
    - commitApply(plan) without choices: all toApply items written, no conflict items written (unchanged Phase 25 behavior)
    - commitApply(plan, choices) with choices:
      - toApply items: written as before (no change)
      - conflict items with choice 'overwrite': written per their kind-specific logic
      - conflict items with choice 'skip' (or no entry in choices): skipped entirely (default ?? 'skip')
    - Overwrite branch for map_key_collision: merged rows = existing rows updated with block row where key matches overwrite choice + non-colliding rows appended; passed to mapReplaceRegistry
    - Overwrite branch for oneof_dirty_subfield: setValue(`${item.fieldName}.${item.subFieldName}`, blockSubValue, { shouldDirty: false }) — dotted path fine-grained write
    - Overwrite branch for oneof_branch_switch: setValue(item.fieldName, { _selected: item.blockBranch, [item.blockBranch]: item.blockValue }, { shouldDirty: false }) — single atomic call (Pitfall A)
    - Pitfall D fix: ALL existing setValue calls in commitApply (including the scalar/enum/well_known branch) gain explicit { shouldDirty: false } option — even if RHF defaults to false, it must be explicit
    - 'oneof' kind items in toApply use dotted fieldName (e.g. "payment.card_number") — these are same-branch clean sub-fields; write with setValue(item.fieldName, item.value, { shouldDirty: false })
  </behavior>

  <action>
    Read ProtoFormRenderer.tsx lines 1-50 and 155-195 before editing. Then:

    1. Add ConflictChoices to the import from "@/lib/blockApply":
       Find the existing import line for blockApply types and add ConflictChoices and ConflictItem to it.

    2. Update the commitApply function in the applyBlockRef useEffect (currently at lines 172-188). Replace the entire commitApply body:

       The new signature: `commitApply: (plan, choices) => {`

       Phase A — write toApply items (same as before, but add { shouldDirty: false } to setValue):
       For each item in plan.toApply:
       - if item.kind === "map": call mapReplaceRegistry.current[item.fieldName]?.(item.value as unknown[])   (unchanged — map replace already handles dirty state via useFieldArray)
       - else (scalar / enum / well_known / oneof dotted-path): call methods.setValue(item.fieldName, item.value as Parameters<typeof methods.setValue>[1], { shouldDirty: false })   ← ADD { shouldDirty: false } here (Pitfall D fix)

       Phase B — write conflict items where choice is 'overwrite' (only when choices arg is provided):
       If choices is undefined or empty: skip Phase B entirely.
       For each item in plan.conflicts:
         Compute choiceKey:
           - map_key_collision: `${item.fieldName}:${item.collisionKey}`
           - oneof_dirty_subfield: `${item.fieldName}:${item.subFieldName}`
           - oneof_branch_switch: `${item.fieldName}`
         If (choices[choiceKey] ?? 'skip') !== 'overwrite': continue

         Execute per kind:

         map_key_collision:
           - Get current rows from methods.getValues(item.fieldName) as Array<{key: unknown, value: unknown}>
           - Build merged rows:
             1. Take existing rows; for each row, if String(row.key) === String(item.collisionKey), replace it with item.blockValue (the block row)
             2. But only replace if this specific collisionKey has overwrite choice (it does — we already checked)
             3. Note: item.blockValue is the single block row that collided; item.collisionKey identifies it
             4. Merged = existing.map(r => String((r as any).key) === String(item.collisionKey) ? item.blockValue : r)
           - Call mapReplaceRegistry.current[item.fieldName]?.(merged as unknown[])

         oneof_dirty_subfield:
           - Call methods.setValue(`${item.fieldName}.${item.subFieldName}`, item.blockValue as Parameters<typeof methods.setValue>[1], { shouldDirty: false })

         oneof_branch_switch:
           - The blockBranch sub-value: item.blockValue is the full block oneof object { _selected, [branch]: subValue }; extract: const subValue = (item.blockValue as Record<string, unknown>)[item.blockBranch!]
           - Call methods.setValue(item.fieldName, { _selected: item.blockBranch, [item.blockBranch!]: subValue } as Parameters<typeof methods.setValue>[1], { shouldDirty: false })
           - This is the single atomic setValue mandated by Pitfall A and D-05

    3. CRITICAL: Do NOT modify mapReplaceRegistry, the buildPlan function, the useEffect dependency array, or the applyBlockRef cleanup return. Only the commitApply body changes.

    4. The ApplyBlockRef type was updated in Plan 26-01 to `commitApply: (plan: ApplyPlan, choices?: ConflictChoices) => void`. ProtoFormRenderer's inline implementation must now match this signature — TypeScript will enforce it via the type assignment `applyBlockRef.current = { buildPlan, commitApply }`.
  </action>

  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "ProtoFormRenderer|blockApply|commitApply" | head -20</automated>
    No TypeScript errors.
    <automated>npx vitest run src/lib/blockApply.test.ts 2>&1 | tail -10</automated>
    All tests still pass (pure function tests unaffected by renderer changes).
    <automated>npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
    Output: 0
  </verify>

  <done>
    ProtoFormRenderer.tsx: commitApply accepts optional ConflictChoices param; all three overwrite branches implemented; { shouldDirty: false } explicit on all setValue calls; atomic oneof write for branch-switch; TypeScript compiles without errors; vitest suite still all green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    End-to-end conflict dialog flow:
    - Plan 26-01: buildApplyPlan detects map key collisions, oneof dirty subfields, and branch-switch conflicts
    - Plan 26-02 Task 1: FormPanel shows BlockApplyConflictDialog when conflicts exist
    - Plan 26-02 Task 2: commitApply writes conflict resolutions (overwrite/skip per row)
  </what-built>
  <how-to-verify>
    1. Run the app: `npm run tauri dev`

    Scenario A — map key collision (BLK-EXT-03):
    a. Load a .proto file with a map field (e.g. labels: map<string, string>)
    b. Add an existing map entry with key "env"
    c. Create a block with JSON: { "labels": [{ "key": "env", "value": "staging" }, { "key": "team", "value": "infra" }] }
    d. Drag the block onto the form drop zone
    e. Expected: Conflict dialog opens with one row for key "env" (kind badge: "map key"), defaulting to Skip
    f. Set the "env" row to Overwrite, click "Apply block"
    g. Expected: "env" key value updated to "staging"; "team" key added (non-colliding); no extra entries; dialog closes

    Scenario B — oneof branch-switch (BLK-EXT-05):
    a. Load a .proto file with a oneof field (e.g. payment: oneof { string card_number = 1; string bank_transfer = 2; })
    b. Select card_number branch and fill it
    c. Create a block with JSON: { "payment": { "_selected": "bank_transfer", "bank_transfer": "IBAN123" } }
    d. Drag block onto form
    e. Expected: Dialog opens with one row — badge "branch switch", label "Switch 'payment' from 'card_number' to 'bank_transfer'"
    f. Choose Overwrite, click Apply block
    g. Expected: oneof field switches to bank_transfer branch with value "IBAN123"; dialog closes

    Scenario C — no conflicts (regression guard):
    a. Load form with empty fields
    b. Create a block with JSON matching scalar/enum fields that are not dirty
    c. Drag block
    d. Expected: No dialog appears; fields filled immediately (Phase 25 behavior unchanged)

    Scenario D — Discard block:
    a. Trigger scenario A or B to open the dialog
    b. Click "Discard block"
    c. Expected: Dialog closes; form values unchanged

    Scenario E — drag same block twice (Pitfall D regression check):
    a. Apply a block with no conflicts (Scenario C)
    b. Drag the same block again immediately after
    c. Expected: No dialog on second drag (fields were applied with shouldDirty:false, so they are NOT dirty; second drag also applies cleanly)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found during UAT</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Block JSON → buildPlan → dialog | Block content (user-authored JSON) is parsed and reflected in conflict row labels and value previews |
| Dialog choices → commitApply | User radio choices (skip/overwrite strings) control which form values are written |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Tampering | commitApply — conflict choice execution | accept | Choices are local React state (Record<string, 'skip'\|'overwrite'>); only two valid values per key; no network or persistence involved; malformed choices default to 'skip' via ?? 'skip' guard |
| T-26-04 | Information Disclosure | ConflictRow — currentValue preview | accept | currentValue is local RHF form state; displayed only in a synchronous dialog visible to the local user only; no PII leaves the process |
| T-26-05 | Denial of Service | ScrollArea — many conflict rows | accept | max-h-[50vh] caps dialog height; list renders only on drag which requires deliberate user action; no unbounded rendering path |
| T-26-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; all UI primitives already in inventory |
</threat_model>

<verification>
Full test suite must pass:
  npx vitest run

TypeScript must compile cleanly:
  npx tsc --noEmit

Manual UAT scenarios documented in Task 3 (human-verify checkpoint).
</verification>

<success_criteria>
- npx vitest run — all tests pass, zero failures
- npx tsc --noEmit — exits 0, no errors
- BlockApplyConflictDialog appears on drag when plan.conflicts.length > 0
- Dialog skipped entirely when plan.conflicts.length === 0 (Phase 25 behavior)
- Discard block closes dialog without writing any form values
- Apply block with default choices (all skip) writes only toApply items
- Apply block with overwrite choice for a conflict item writes that item
- Drag same block twice with no edits between: no false conflict on second drag (Pitfall D)
- Atomic oneof branch-switch write uses single setValue call (Pitfall A)
</success_criteria>

<output>
Create `.planning/phases/26-block-apply-conflict-prompt-oneof/26-02-SUMMARY.md` when done
</output>
