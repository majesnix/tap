---
phase: 17-macos-signing-notarization
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - docs/release-setup.md
autonomous: false
requirements:
  - CICD-01
  - SIGN-01
  - SIGN-02

user_setup:
  - service: apple-developer-portal
    why: "Developer ID Application certificate required for code signing"
    dashboard_config:
      - task: "Register App ID com.tap.app"
        location: "developer.apple.com → Certificates, Identifiers & Profiles → Identifiers"
      - task: "Create Developer ID Application certificate"
        location: "developer.apple.com → Certificates, Identifiers & Profiles → Certificates → +"
      - task: "Download .cer file and double-click to install in Keychain Access"
        location: "Keychain Access → My Certificates"
      - task: "Export certificate as .p12 (right-click → Export) with a strong password"
        location: "Keychain Access → My Certificates"
  - service: appleid.apple.com
    why: "App-specific password required for notarytool (Apple ID account password not accepted)"
    dashboard_config:
      - task: "Generate app-specific password named 'tap-notarize'"
        location: "appleid.apple.com → Sign-In and Security → App-Specific Passwords"
  - service: github
    why: "9 GitHub secrets required for signing and notarization pipeline"
    env_vars:
      - name: APPLE_CERTIFICATE
        source: "base64 of exported .p12 file: `base64 -i certificate.p12 | pbcopy`"
      - name: APPLE_CERTIFICATE_PASSWORD
        source: "Password used when exporting the .p12 from Keychain Access"
      - name: APPLE_SIGNING_IDENTITY
        source: "Full identity string from Keychain Access, e.g. 'Developer ID Application: Your Name (TEAMID)'"
      - name: APPLE_ID
        source: "Your Apple ID email address (e.g. you@example.com)"
      - name: APPLE_PASSWORD
        source: "App-specific password generated at appleid.apple.com (NOT your Apple ID account password)"
      - name: APPLE_TEAM_ID
        source: "10-character team ID from developer.apple.com → Account → Membership"
      - name: KEYCHAIN_PASSWORD
        source: "Generate a random string: `openssl rand -base64 32` — used only for the ephemeral CI keychain"
      - name: TAURI_SIGNING_PRIVATE_KEY
        source: "Required for Phase 18 auto-update. Generate: `tauri signer generate -w ~/.tauri/tap.key` then paste the public key content"
      - name: TAURI_SIGNING_PRIVATE_KEY_PASSWORD
        source: "Password set when generating the signer key above"

must_haves:
  truths:
    - "docs/release-setup.md exists with all 9 secrets listed and how to obtain each"
    - "KEYCHAIN_PASSWORD is explicitly listed with the openssl rand -base64 32 generation command"
    - "Checklist covers all prerequisite human steps: Apple Developer portal, appleid.apple.com, GitHub Secrets"
    - "A signed notarized .dmg appears as an artifact in a draft GitHub Release after pushing a v1.5.0 tag"
    - "spctl --assess confirms Notarized Developer ID on the downloaded .dmg"
  artifacts:
    - path: "docs/release-setup.md"
      provides: "Human setup checklist for all 9 GitHub secrets"
      contains: "KEYCHAIN_PASSWORD"
  key_links:
    - from: "docs/release-setup.md"
      to: "github.com/majesnix/proto-sender/settings/secrets/actions"
      via: "Manual step: user opens GitHub repo settings and adds each secret"
      pattern: "KEYCHAIN_PASSWORD"
---

<objective>
Create the human setup documentation for the 9 GitHub secrets required by the signing pipeline, then execute the first signed tag-push and verify the resulting .dmg passes Gatekeeper on a clean Mac.

Purpose: The code changes from Plan 1 cannot be tested until the secrets exist in GitHub. This plan produces the checklist, checkpoints the user through secret setup and tag push, and verifies the Gatekeeper outcome.

Output: `docs/release-setup.md` with the complete human setup checklist. After the checkpoint sequence: a draft GitHub Release with a signed, notarized .dmg attached.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-macos-signing-notarization/17-01-SUMMARY.md
@.planning/phases/17-macos-signing-notarization/17-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write human setup checklist to docs/release-setup.md</name>
  <files>docs/release-setup.md</files>
  <read_first>
    - .planning/phases/17-macos-signing-notarization/17-RESEARCH.md — sections: "Runtime State Inventory" (9-secret list), "Open Questions" (KEYCHAIN_PASSWORD), "Environment Availability" (human prerequisites)
    - .planning/ROADMAP.md — Phase 17 "Depends on" block (lists 8 secrets from original ROADMAP; plan adds the 9th)
  </read_first>
  <action>
Create `docs/release-setup.md`. The directory `docs/` may not exist — create it if needed.

The file must be a practical, step-by-step setup guide for the developer. It covers ALL prerequisites required before pushing a signed tag.

Structure:

```markdown
# macOS Signing + Notarization Setup

One-time setup required before pushing a signed release tag.

## Prerequisites

- Apple Developer Program membership (paid, $99/year)
- macOS machine with Xcode installed for certificate export

---

## Step 1: Register App ID

1. Go to [developer.apple.com → Certificates, Identifiers & Profiles → Identifiers](https://developer.apple.com/account/resources/identifiers/list)
2. Click **+** and choose **App IDs**
3. Select **App** type
4. Set Bundle ID (Explicit): `com.tap.app`
5. No capabilities needed — click **Continue** then **Register**

---

## Step 2: Create Developer ID Application Certificate

1. Go to [Certificates, Identifiers & Profiles → Certificates → +](https://developer.apple.com/account/resources/certificates/add)
2. Choose **Developer ID Application**
3. Follow the prompts to generate a Certificate Signing Request (CSR) via Keychain Access
4. Download the resulting `.cer` file
5. Double-click the `.cer` to install it in Keychain Access
6. In Keychain Access, under **My Certificates**, find **Developer ID Application: Your Name (TEAMID)**
7. Right-click → **Export** → save as `certificate.p12` with a strong password (save this password — it becomes `APPLE_CERTIFICATE_PASSWORD`)

---

## Step 3: Generate App-Specific Password

1. Go to [appleid.apple.com → Sign-In and Security → App-Specific Passwords](https://appleid.apple.com/account/manage)
2. Click **+** and name it `tap-notarize`
3. Copy the generated password (it will not be shown again) — this becomes `APPLE_PASSWORD`

**Important:** `APPLE_PASSWORD` is the app-specific password, NOT your Apple ID account password. notarytool rejects Apple ID passwords.

---

## Step 4: Find Your Team ID

1. Go to [developer.apple.com → Account → Membership](https://developer.apple.com/account/#/membership)
2. Copy the **Team ID** (10-character alphanumeric string) — this becomes `APPLE_TEAM_ID`

---

## Step 5: Generate KEYCHAIN_PASSWORD

The release pipeline creates a temporary CI keychain to hold the certificate during each build run. Generate a random password for it:

```bash
openssl rand -base64 32
```

Copy the output — this becomes `KEYCHAIN_PASSWORD`. It has no external dependency; it only secures the ephemeral CI keychain on the runner.

---

## Step 6: Add All 9 Secrets to GitHub

Go to [github.com/majesnix/proto-sender/settings/secrets/actions](https://github.com/majesnix/proto-sender/settings/secrets/actions) and add each secret:

| Secret Name | Value |
|-------------|-------|
| `APPLE_CERTIFICATE` | Base64-encoded .p12: `base64 -i certificate.p12 \| pbcopy` (paste the result) |
| `APPLE_CERTIFICATE_PASSWORD` | Password used when exporting the .p12 in Step 2 |
| `APPLE_SIGNING_IDENTITY` | Full identity string, e.g. `Developer ID Application: Your Name (XXXXXXXXXX)` — copy from Keychain Access |
| `APPLE_ID` | Your Apple ID email address |
| `APPLE_PASSWORD` | App-specific password from Step 3 |
| `APPLE_TEAM_ID` | 10-character Team ID from Step 4 |
| `KEYCHAIN_PASSWORD` | Random string from Step 5 |
| `TAURI_SIGNING_PRIVATE_KEY` | Required for Phase 18 auto-update. Generate: `tauri signer generate -w ~/.tauri/tap.key` — paste the private key file content |
| `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` | Password chosen when generating the signer key above |

---

## Step 7: Push a Signed Tag

After all 9 secrets are in place:

```bash
git tag v1.5.0
git push origin v1.5.0
```

The GitHub Actions release pipeline will:
1. Build the app for universal macOS (arm64 + x86_64)
2. Import your certificate into the CI keychain
3. Sign the `.app` bundle with your Developer ID
4. Submit to Apple's notary service and wait for approval
5. Staple the notarization ticket to the `.dmg`
6. Run `spctl --assess` to verify Gatekeeper approval
7. Upload the `.dmg` as a draft GitHub Release

---

## Verification

After the pipeline completes (typically 15–25 minutes):

1. Open [github.com/majesnix/proto-sender/releases](https://github.com/majesnix/proto-sender/releases) — a draft release should appear
2. Download the `.dmg` to a clean Mac (one that has never seen this binary)
3. Mount the `.dmg` and attempt to open `Tap.app` — no Gatekeeper warning should appear
4. To verify programmatically:

```bash
# Check code signature
codesign -dv --verbose=4 /Volumes/Tap/Tap.app 2>&1 | grep -E "Authority|Identifier|Architecture"

# Check notarization (Gatekeeper)
spctl --assess --type open --context context:primary-signature --verbose /Volumes/Tap/Tap.app

# Check Universal binary
lipo -info /Volumes/Tap/Tap.app/Contents/MacOS/tap
```

Expected outputs:
- `codesign`: `Authority=Developer ID Application: ...`
- `spctl`: `accepted source=Notarized Developer ID`
- `lipo`: `Architectures in the fat file: ... are: x86_64 arm64`
```

Write this content exactly. Ensure `docs/` directory is created if absent.
  </action>
  <verify>
    <automated>test -f docs/release-setup.md && grep -c "KEYCHAIN_PASSWORD" docs/release-setup.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/release-setup.md` exits 0 (file exists)
    - `grep -c "KEYCHAIN_PASSWORD" docs/release-setup.md` returns at least 3 (appears in table, step 5 heading, and generation command)
    - `grep "openssl rand -base64 32" docs/release-setup.md` matches (KEYCHAIN_PASSWORD generation command present)
    - `grep "APPLE_TEAM_ID" docs/release-setup.md` matches (secret in table)
    - `grep "TAURI_SIGNING_PRIVATE_KEY" docs/release-setup.md` matches (both Phase 18 secrets documented)
    - `grep "app-specific password" docs/release-setup.md` matches (APPLE_PASSWORD warning present)
    - `grep "9 secrets" docs/release-setup.md` or the secrets table has 9 rows: `grep -c "^\| \`" docs/release-setup.md` returns 9
    - `grep "spctl --assess" docs/release-setup.md` matches (verification instructions present)
    - `grep "Notarized Developer ID" docs/release-setup.md` matches (expected spctl output documented)
  </acceptance_criteria>
  <done>docs/release-setup.md exists with all 9 secrets documented (KEYCHAIN_PASSWORD included with generation command), step-by-step Apple Developer portal instructions, and post-push verification commands with expected outputs.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Human — add 9 GitHub secrets and push v1.5.0 tag</name>
  <what-built>docs/release-setup.md with complete setup instructions for all 9 secrets and the tag-push command</what-built>
  <how-to-verify>
    Complete the following steps IN ORDER. Do not push the tag until all 9 secrets are added.

    **Step A — Complete Apple Developer setup (if not already done):**
    Follow Steps 1–5 in `docs/release-setup.md`:
    1. Register App ID `com.tap.app` at developer.apple.com
    2. Create Developer ID Application certificate, export as .p12
    3. Generate app-specific password at appleid.apple.com → tap-notarize
    4. Note your 10-character Team ID
    5. Generate KEYCHAIN_PASSWORD: `openssl rand -base64 32`

    **Step B — Add all 9 secrets to GitHub:**
    Open https://github.com/majesnix/proto-sender/settings/secrets/actions and add:
    - `APPLE_CERTIFICATE` (base64 of .p12: `base64 -i certificate.p12 | pbcopy`)
    - `APPLE_CERTIFICATE_PASSWORD`
    - `APPLE_SIGNING_IDENTITY` (e.g. "Developer ID Application: Your Name (XXXXXXXXXX)")
    - `APPLE_ID`
    - `APPLE_PASSWORD` (app-specific password — NOT your Apple ID password)
    - `APPLE_TEAM_ID`
    - `KEYCHAIN_PASSWORD`
    - `TAURI_SIGNING_PRIVATE_KEY` (can be a placeholder for now — required by Phase 18 but not signing)
    - `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` (can be a placeholder for now)

    **Step C — Push the tag:**
    ```bash
    git tag v1.5.0
    git push origin v1.5.0
    ```

    **Step D — Monitor the pipeline:**
    Watch https://github.com/majesnix/proto-sender/actions — the release workflow should start within 30 seconds.
    Expected duration: 15–25 minutes (Apple notarization takes 5–15 minutes).

    **Step E — Report the result:**
    When the pipeline completes, report one of:
    - "pipeline green — draft release created" (continue to Task 3)
    - "pipeline failed at [step name] — [error message]" (Claude will diagnose)
  </how-to-verify>
  <resume-signal>Type "pipeline green — draft release created" or describe the failure step and error message</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human — verify Gatekeeper approval on clean Mac</name>
  <what-built>A draft GitHub Release containing the signed, notarized .dmg artifact produced by the release pipeline</what-built>
  <how-to-verify>
    On a Mac that has NEVER opened a previous version of Tap (or with a fresh user account / VM):

    1. Open https://github.com/majesnix/proto-sender/releases — find the v1.5.0 draft release
    2. Download the `.dmg` file
    3. Mount the `.dmg` (double-click)
    4. Attempt to open `Tap.app` — **no Gatekeeper warning should appear and the app should launch**

    Then run these verification commands:
    ```bash
    # Replace /Volumes/Tap with the actual mount path if different
    codesign -dv --verbose=4 /Volumes/Tap/Tap.app 2>&1 | grep -E "Authority|Identifier"
    spctl --assess --type open --context context:primary-signature --verbose /Volumes/Tap/Tap.app
    lipo -info /Volumes/Tap/Tap.app/Contents/MacOS/tap
    ```

    Expected outputs:
    - `codesign`: line containing `Authority=Developer ID Application: ...`
    - `spctl`: output containing `accepted source=Notarized Developer ID`
    - `lipo`: output containing both `x86_64` and `arm64`

    **ROADMAP success criteria covered by this checkpoint:**
    - SC-1: draft release with .dmg artifact ✓ (verified in Task 2)
    - SC-2: Developer ID Application authority + Universal binary ✓
    - SC-3: `spctl` returns `accepted source=Notarized Developer ID` ✓
    - SC-4: No quarantine warning on clean Mac, app launches ✓
  </how-to-verify>
  <resume-signal>Type "gatekeeper approved — all three commands show expected output" or describe any failure (which command failed and what output was shown)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → docs/release-setup.md | Sensitive setup instructions (secret names and locations); file is committed to repo |
| GitHub Secrets UI → CI runner | 9 secrets entered manually by developer; any typo or wrong value causes signing failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Information Disclosure | docs/release-setup.md committed to public repo | accept | File documents WHERE to obtain secrets (Apple portal, appleid.apple.com) — it does NOT contain actual secret values; analogous to README installation instructions |
| T-17-08 | Tampering | Wrong value for APPLE_SIGNING_IDENTITY causes unsigned build silently shipped | mitigate | CI `spctl --assess` step (Plan 1 Task 1) gates the pipeline; draft release does not auto-publish |
| T-17-09 | Spoofing | Developer pushes tag before secrets are added | mitigate | `if: github.event_name == 'push'` guards on cert steps; tauri-action skips signing when APPLE_SIGNING_IDENTITY is empty; spctl step will fail the build exposing the issue |
</threat_model>

<verification>
After Task 1 (automated):
1. `test -f docs/release-setup.md` exits 0
2. `grep -c "KEYCHAIN_PASSWORD" docs/release-setup.md` returns ≥ 3
3. `grep -c "openssl rand" docs/release-setup.md` returns 1

After Tasks 2–3 (human):
4. GitHub Release at tag v1.5.0 exists in draft state with .dmg attached
5. `spctl --assess --type open --context context:primary-signature --verbose Tap.app` returns `accepted source=Notarized Developer ID`
6. `lipo -info Tap.app/Contents/MacOS/tap` shows both `x86_64` and `arm64`
7. App opens on clean Mac without Gatekeeper warning
</verification>

<success_criteria>
- docs/release-setup.md exists with all 9 secrets documented and KEYCHAIN_PASSWORD generation command
- Tag push v1.5.0 produces a draft GitHub Release with .dmg artifact
- spctl --assess returns accepted source=Notarized Developer ID
- lipo confirms Universal binary (arm64 + x86_64)
- App opens on clean Mac with no Gatekeeper quarantine warning
- All four Phase 17 ROADMAP success criteria verified (SC-1 through SC-4)
</success_criteria>

<output>
After completion, create `.planning/phases/17-macos-signing-notarization/17-02-SUMMARY.md`
</output>
