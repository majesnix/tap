syntax = "proto3";

package common;

// Postal address — country_code is ISO 3166-1 alpha-2 (e.g. "US", "DE").
message Address {
  string street_line1 = 1;
  string street_line2 = 2;
  string city         = 3;
  string state        = 4;
  string postal_code  = 5;
  string country_code = 6;
}
